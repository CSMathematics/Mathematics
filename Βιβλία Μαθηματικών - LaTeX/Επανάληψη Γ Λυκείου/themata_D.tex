\section{Θέματα Δ}
\def\xrwma{thirdcolor}
%=========== 1ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = x\ln x - x + 1$, με $x > 0$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και τα ακρότατα, και να βρείτε το σύνολο τιμών της.
    \item Να αποδείξετε ότι η γραφική παράσταση της $f$ δέχεται ακριβώς μία εφαπτομένη η οποία διέρχεται από το σημείο $A(0, -1)$, και να βρείτε την εξίσωσή της.
    \item Να αποδείξετε ότι $x\ln x \ge x - 1$ για κάθε $x > 0$, και στη συνέχεια να λύσετε στο $(0, +\infty)$ την εξίσωση $f(x^2) + f(x^3) = 0$.
    \item Έστω $F$ μία παράγουσα (αρχική) της $f$ στο $(0, +\infty)$. Να υπολογίσετε το όριο:
    \[ \lim_{x\to 1} \frac{F(x) - F(1)}{(x-1)^3} \]
\end{erwthma}
\end{thema}

%=========== ΤΕΛΟΣ - 1ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 2ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η συνάρτηση $f(x) = e^{2x} - \ln(x+1) - 1$, ορισμένη στο $(-1, +\infty)$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι αυστηρά κυρτή στο $(-1, +\infty)$.
    \item Να αποδείξετε ότι η εξίσωση $f'(x) = 0$ έχει ακριβώς μία ρίζα $x_0 \in \left(-\frac{1}{2}, 0\right)$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, να βρείτε το είδος του ακροτάτου της και να αποδείξετε ότι $f(x) \ge f(x_0)$ για κάθε $x > -1$.
    \item Αν $F$ είναι μια παράγουσα (αρχική) της $f$ στο $(-1, +\infty)$, να δείξετε ότι για κάθε $x > 0$ ισχύει: $F(3x) + F(x) > 2F(2x)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 2ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 3ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνεχής συνάρτηση $f: \mathbb{R} \to \mathbb{R}$ για την οποία ισχύει:
\[ f^3(x) + 3f(x) = x + 4, \quad \text{για κάθε } x \in \mathbb{R} \]
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ είναι συνάρτηση 1-1 και να βρείτε την αντίστροφή της $f^{-1}$.
    \item Να μελετήσετε την $f^{-1}$ ως προς τη μονοτονία και να βρείτε τα κοινά σημεία των γραφικών παραστάσεων $C_f$ και $C_{f^{-1}}$.
    \item Να υπολογίσετε το όριο $\displaystyle{\lim_{x\to +\infty} \frac{f(x)}{\sqrt[3]{x}}}$.
    \item Να υπολογίσετε το ολοκλήρωμα $\displaystyle\int_{-4}^{0} f(x)\,dx$, χρησιμοποιώντας κατάλληλη αλλαγή μεταβλητής.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 3ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 4ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = \dfrac{x^2}{x^2+1}$ με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να βρείτε τις ασύμπτωτες της $C_f$, να μελετήσετε τη μονοτονία της $f$ και να βρείτε το σύνολο τιμών της.
    \item Να μελετήσετε την κυρτότητα της $f$ και να προσδιορίσετε τα σημεία καμπής της.
    \item Να αποδείξετε ότι για κάθε $x \neq 0$ ισχύει $f(x) + f\!\left(\dfrac{1}{x}\right) = 1$, και να συμπεράνετε ότι η συνάρτηση $g(x) = f(x) - \dfrac{1}{2}$ είναι περιττή.
    \item Χρησιμοποιώντας την ιδιότητα του ερωτήματος (γ) και την αλλαγή μεταβλητής $x = \dfrac{1}{t}$, να υπολογίσετε, για κάθε $a > 1$, το ολοκλήρωμα $\displaystyle\int_{1/a}^{a} f(x)\,dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 4ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 5ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = x e^x - e^x + 1$, με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και να λύσετε την εξίσωση $f(x) = 0$.
    \item Να αποδείξετε ότι η $C_f$ δέχεται οριζόντια ασύμπτωτη $y = 1$ στο $-\infty$, και στη συνέχεια να συζητήσετε τον αριθμό των πραγματικών ριζών της εξίσωσης $f(x) = c$, ανάλογα με την τιμή του $c \in \mathbb{R}$.
    \item Να αποδείξετε ότι η εξίσωση $x e^x - e^x + 1 = 2026$ έχει ακριβώς μία ρίζα στο $\mathbb{R}$.
    \item Έστω $F$ μία παράγουσα της $f$ με $F(0) = 0$. Να υπολογίσετε το όριο:
    \[ \lim_{x \to 0} \frac{F(x)}{x^3} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 5ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 6ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω μια παραγωγίσιμη συνάρτηση $f: (0, +\infty) \to \mathbb{R}$ για την οποία ισχύει:
\[ x f'(x) + f(x) = x e^x, \quad \text{για κάθε } x > 0 \quad \text{και} \quad f(1) = e \]
\begin{erwthma}
    \item Να αποδείξετε ότι ο τύπος της συνάρτησης είναι $f(x) = \frac{x e^x - e^x + e}{x}$.
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι γνησίως αύξουσα.
    \item Να υπολογίσετε τα όρια $\displaystyle\lim_{x\to 0^+} f(x)$ και $\displaystyle\lim_{x\to +\infty} f(x)$.
    \item Να δείξετε ότι η εφαπτομένη της γραφικής παράστασης $C_f$ στο $x_0 = 1$ τέμνει τον άξονα $y'y$ στο σημείο με τεταγμένη ίση με το $\lim_{x\to 0^+} f(x)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 6ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 7ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε τη συνάρτηση $f(x) = \ln\left( x + \sqrt{x^2+1} \right)$, με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ είναι περιττή συνάρτηση και ότι η $C_f$ διέρχεται από την αρχή των αξόνων.
    \item Να μελετήσετε τη μονοτονία της $f$ και να δείξετε ότι $f'(x) = \frac{1}{\sqrt{x^2+1}}$. 
    \item Να συζητήσετε την κυρτότητα της $f$ και να αναδείξετε ένα μοναδικό σημείο καμπής.
    \item Να λύσετε την ανίσωση $f(x^2 - 4) + f(x - 2) < 0$.
    \item Χρησιμοποιώντας τα αποτελέσματα των προηγούμενων ερωτημάτων, να αποδείξετε ότι $f(x) \leq x$ για κάθε $x \geq 0$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 7ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 8ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = e^{-x} \hm x$, με $x \in [0, \pi]$.
\begin{erwthma}
    \item Να βρείτε τις ρίζες της $f$ στο διάστημα $[0, \pi]$.
    \item Να μελετήσετε την $f$ ως προς τα τοπικά ακρότατα στο $[0, \pi]$ και να αποδείξετε ότι η μέγιστη τιμή της είναι $e^{-\pi/4} \frac{\sqrt{2}}{2}$.
    \item Να αποδείξετε ότι το μέγιστο της $f$ στο $[0, \pi]$ επιτυγχάνεται στο εσωτερικό του διαστήματος, και να συμπεράνετε ότι $f(x) \leq e^{-\pi/4}\dfrac{\sqrt{2}}{2}$ για κάθε $x \in [0, \pi]$.
    \item Να υπολογίσετε το ορισμένο ολοκλήρωμα $\dint_0^\pi f(x) dx$ χρησιμοποιώντας διαδοχική ολοκλήρωση κατά παράγοντες.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 8ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 9ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω $f: \mathbb{R} \to \mathbb{R}$ μια δύο φορές παραγωγίσιμη συνάρτηση για την οποία ισχύει: $f(0)=f(1)=0$ και $f''(x) > 0$ για κάθε $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι $f(x) < 0$ για κάθε $x \in (0, 1)$.
    \item Να δείξετε ότι υπάρχει ακριβώς ένα $x_0 \in (0, 1)$ τέτοιο ώστε η εφαπτομένη της $C_f$ στο $x_0$ να είναι παράλληλη με τον άξονα $x'x$.
    \item Να αποδείξετε ότι η $f$ παρουσιάζει ένα μοναδικό ολικό ελάχιστο.
    \item Αν επιπλέον $f'(0)=-1$ και $f'(1)=2$, να υπολογίσετε το ολοκλήρωμα $\dint_0^1 x f''(x) dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 9ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 10ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = \frac{\ln x}{x}$, με $x > 0$.
\begin{erwthma}
    \item Να μελετήσετε τη μονοτονία της $f$, να βρείτε το ολικό μέγιστο και τις ασύμπτωτες της $C_f$.
    \item Με χρήση του παραπάνω ακροτάτου, να αποδείξετε ότι $x^e \le e^x$ για κάθε $x > 0$ και να συγκρίνετε τους αριθμούς $\pi^e$ και $e^\pi$.
    \item Αν $a > \beta \ge e$, να αποδείξετε ότι $\beta^a > a^\beta$.
    \item Να υπολογίσετε το όριο: $\displaystyle \lim_{x \to 0^+} \left( f(x) \cdot \hm x \right)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 10ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 11ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε τη συνάρτηση $f(x) = x^3 - 3x + a$, όπου $a \in \mathbb{R}$.
\begin{erwthma}
    \item Να βρείτε τα τοπικά ακρότατα της $f$ με βάση τη μονοτονία της.
    \item Να προσδιορίσετε τις τιμές του πραγματικού αριθμού $a$, ώστε η εξίσωση $f(x) = 0$ να έχει ακριβώς τρεις πραγματικές, άνισες μεταξύ τους ρίζες.
    \item Έστω ότι $a=2$. Να αποδείξετε ότι η $C_f$ εφάπτεται στον άξονα $x'x$ στο σημείο $A(1,0)$ και να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο καμπής της.
    \item Για $a=2$, να υπολογίσετε το όριο:
    \[ \lim_{x \to 1} \frac{....}{(x-1)^3} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 11ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 12ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η συνάρτηση $f(x) = -x + \sqrt{x^2+1}$ για $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να βρείτε την παράγωγο $f'$ και να αποδείξετε ότι $f'(x) < 0$, άρα η $f$ είναι γνησίως φθίνουσα στο $\mathbb{R}$.
    \item Να υπολογίσετε το $\lim_{x\to +\infty} f(x)$ και να δείξετε ότι η ευθεία $y=0$ είναι οριζόντια ασύμπτωτη.
    \item Να βρείτε το $\lim_{x\to -\infty} f(x)$ και να δείξετε ότι η ευθεία $y = -2x$ είναι πλάγια ασύμπτωτη της $C_f$ στο $-\infty$.
    \item Να αποδείξετε ότι $f(x) \cdot f(-x) = 1$ για κάθε $x \in \mathbb{R}$ και στη συνέχεια να υπολογίσετε το ολοκλήρωμα:
    \[ I = \dint_{-1}^1 \frac{1}{1+f(x)} dx \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 12ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 13ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω παραγωγίσιμη συνάρτηση $f: \mathbb{R} \to \mathbb{R}$ τέτοια ώστε:
\[ x f'(x) + f(x) + e^x = 0 \quad \text{για κάθε } x \ne 0 \quad \text{και} \quad f(1) = 1-e \]
\begin{erwthma}
    \item Να αποδείξετε ότι $(x f(x) + e^x)' = 0$ για κάθε $x \neq 0$ και να βρείτε τον τύπο της $f(x)$ για $x \ne 0$.
    \item Έστω $f(x) = \frac{1 - e^x}{x}$ για $x \ne 0$. Αφού η $f$ είναι συνεχής στο $\mathbb{R}$, να βρείτε την τιμή $f(0)$.
    \item Να δείξετε ότι η $f$ είναι γνησίως φθίνουσα.
    \item Να υπολογιστεί το όριο $\displaystyle \lim_{x \to -\infty} f(x)$  και να προσδιοριστεί η ασύμπτωτη.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 13ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 14ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η παραγωγίσιμη συνάρτηση $f:(0, +\infty)\to\mathbb{R}$ για την οποία ισχύει: $f'(x) = \frac{\ln x}{x^2}$ και $f(1)=0$.
\begin{erwthma}
    \item Να βρείτε τον τύπο της $f(x)$, χρησιμοποιώντας ολοκλήρωση κατά παράγοντες και την αρχική συνθήκη $f(1)=0$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, να βρείτε το τοπικό ελάχιστο και να υπολογίσετε την ελάχιστη τιμή της $f$.
    \item Να δείξετε ότι η εξίσωση $f(x) = \dfrac{1}{2}$ έχει ακριβώς δύο ρίζες στο $(0, +\infty)$.
    \item Να υπολογίσετε το ολοκλήρωμα $\displaystyle\int_1^e f(x)\,dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 14ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 15ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε τη συνάρτηση $f(x) = x^2e^{-x}$.
\begin{erwthma}
    \item Να μελετήσετε τη μονοτονία της $f$, να βρείτε τα τοπικά ακρότατα και τις ασύμπτωτες της γραφικής παράστασης.
    \item Να βρείτε τα σημεία καμπής της $f$.
    \item Να δείξετε ότι για κάθε $x \in \mathbb{R}$ ισχύει η ανισότητα $x^2 \leq 4e^{x-2}$.
    \item Αν $E(a) = \displaystyle\int_0^a f(x)\,dx$ για $a > 0$, να αποδείξετε ότι $\displaystyle\lim_{a \to +\infty} E(a) = 2$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 15ο ΘΕΜΑ Δ ===========

%=========== 16ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = \begin{cases} \frac{x \ln x}{x-1}, & 0 < x \ne 1 \\ 1, & x = 1 \end{cases}$
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι συνεχής στο σημείο $x_0 = 1$.
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι παραγωγίσιμη στο $x_0 = 1$ και να βρείτε την $f'(1)$.
    \item Να υπολογίσετε το $\displaystyle\lim_{x \to 0^+} f(x)$ και να αποδείξετε ότι η $f$ επεκτείνεται συνεχώς στο $x=0$ (δεν υπάρχει κατακόρυφη ασύμπτωτη).
    \item Να δείξετε ότι $f'(x) > 0$ για $x > 0$, $x \ne 1$, επομένως η $f$ είναι γνησίως αύξουσα.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 16ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 17ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η δύο φορές παραγωγίσιμη συνάρτηση $f:\mathbb{R}\to\mathbb{R}$, για την οποία $f(0)=0$ και 
\[ f''(x) - 2f'(x) + f(x) = 0 \quad \text{και} \quad f'(0) = 1 \]
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $g(x) = f(x) e^{-x}$ ικανοποιεί $g''(x)=0$ για κάθε $x \in \mathbb{R}$.
    \item Να βρείτε τον τύπο της $f(x)$ καταλήγοντας στον $f(x) = x e^x$.
    \item Να μελετήσετε την $f$ ως προς μονοτονία, ακρότατα, κυρτότητα.
    \item Να λύσετε την ανίσωση $f(x^2) - f(3x) < 0$ στο $\mathbb{R}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 17ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 18ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε την συνάρτηση $f(x) = (x-2)e^x + x + 2$, με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να εξετάσετε την $f$ ως προς τη μονοτονία, να βρείτε τις ρίζες της (αν υπάρχουν) και το πρόσημό της.
    \item Να αποδείξετε ότι η $C_f$ έχει ακριβώς ένα σημείο καμπής, στο οποίο η εφαπτομένη της παρουσιάζει την ελάχιστη δυνατή κλίση.
    \item Να αποδείξετε ότι η ευθεία $(\varepsilon): y = x + 2$ είναι πλάγια ασύμπτωτη της $C_f$ στο $-\infty$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από την $C_f$, την ασύμπτωτη $(\varepsilon)$ και τις ευθείες $x=0$, $x=-1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 18ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 19ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η συνάρτηση $f(x) = \ln(x^2+1) - x$, με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ είναι γνησίως φθίνουσα στο $\mathbb{R}$ και να λύσετε την εξίσωση $f(e^x - 1) = f(0)$.
    \item Να μελετήσετε την $f$ ως προς την κυρτότητα και να βρείτε τα σημεία καμπής της.
    \item Να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο της $A(0, f(0))$ και να αποδείξετε ότι η $C_f$ βρίσκεται "πάνω" από αυτή την εφαπτομένη για κάθε $x \in \mathbb{R}$.
    \item Να υπολογίσετε το όριο: $\displaystyle \lim_{x\to 0} \frac{f(x) + x}{x^2}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 19ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 20ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = \frac{e^x}{x+1}$, με $x > -1$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και να αποδείξετε ότι παρουσιάζει ολικό ελάχιστο.
    \item Να βρείτε το σύνολο τιμών της $f$.
    \item Να λύσετε την εξίσωση $f(x^2) + f(x^4) = 2$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της συνάρτησης $\displaystyle g(x) = \frac{1}{f(x)}$, τους άξονες $x'x, y'y$ και την ευθεία $x=1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 20ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 21ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω συνάρτηση $f: \mathbb{R} \to \mathbb{R}$ με $f(x)>0$ για την οποία ισχύει $f'(x) = f(x) e^x$ για κάθε $x \in \mathbb{R}$ και $f(0) = e$.
\begin{erwthma}
    \item Να αποδείξετε ότι ο τύπος της συνάρτησης είναι $f(x) = e^{e^x}$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και την κυρτότητα, και να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο $A(0, f(0))$.
    \item Να υπολογίσετε το $\displaystyle \lim_{x\to -\infty} f(x)$ και στη συνέχεια, με χρήση της εφαπτομένης, να αποδείξετε ότι $f(x) \ge e(x+1)$ για κάθε $x \in \mathbb{R}$.
    \item Να αποδείξετε ότι η εξίσωση $f(x) + e^x = e + 1$ έχει ακριβώς μία ρίζα.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 21ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 22ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε την συνάρτηση $f(x) = 2x + \ln(x^2+1)$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ αντιστρέφεται, να βρείτε το πεδίο ορισμού της αντίστροφης και να λύσετε την ανίσωση $f(x^2-x) > f(x+3)$.
    \item Να αποδείξετε ότι η εξίσωση $\displaystyle f(x) = \frac{1}{x}$ έχει ακριβώς μία ρίζα στο διάστημα $(0, 1)$.
    \item Να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο $O(0,0)$ και να μελετήσετε την κυρτότητα της $f$ στο διάστημα $[0, 1]$. Στη συνέχεια, να αποδείξετε ότι $\displaystyle \dint_0^1 f(x) dx > 1$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της συνάρτησης $g(x) = x f(x)$, τον άξονα $x'x$ και την ευθεία $x=1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 22ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 23ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η συνάρτηση $f(x) = \left(x + \frac{1}{x}\right) \ln x$, με $x > 0$.
\begin{erwthma}
    \item Να αποδείξετε ότι $(x^2 - 1)\ln x + x^2 + 1 > 0$ για κάθε $x>0$ και, στη συνέχεια, να μελετήσετε την $f$ ως προς τη μονοτονία και να βρείτε το σύνολο τιμών της.
    \item Θεωρούμε τη συνάρτηση $g(x) = \dfrac{f(x)}{x}$ με $x > 0$. Να δείξετε ότι η $g$ είναι γνησίως αύξουσα και στη συνέχεια να αποδείξετε ότι, για οποιονδήποτε πραγματικό αριθμό $\lambda > 0$, η εξίσωση $f(x) = \lambda x$ έχει ακριβώς μία ρίζα στο $(0, +\infty)$.
    \item Να υπολογίσετε το εμβαδόν $E$ του χωρίου που περικλείεται από τη γραφική παράσταση της $g$, τον άξονα $x'x$ και την ευθεία $x=e$.
    \item Να αποδείξετε ότι υπάρχει ακριβώς ένα $x_0 \in (1, e)$ τέτοιο ώστε το σημείο $M(x_0, f(x_0))$ να ανήκει στην ευθεία η οποία διέρχεται από την αρχή των αξόνων $O(0,0)$ και έχει κλίση ίση με $\dfrac{E}{e-1}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 23ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 24ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = (x-1)e^x + 1, \ x \in \mathbb{R}$.
\begin{erwthma}
    \item Να μελετήσετε τη συνάρτηση $f$ ως προς τη μονοτονία, τα ακρότατα και να αποδείξετε ότι $e^x \le \dfrac{1}{1-x}$ για κάθε $x < 1$.
    \item Να βρείτε το πλήθος των ριζών της εξίσωσης $(x-1)e^x + 1 = \lambda$ για τις διάφορες τιμές του $\lambda \in \mathbb{R}$.
    \item Να αποδείξετε ότι υπάρχουν ακριβώς δύο εφαπτόμενες της $C_f$ που διέρχονται από την αρχή των αξόνων $O(0,0)$, εκ των οποίων η μία εφάπτεται σε σημείο με αρνητική τετμημένη $a \in (-2, -1)$.
    \item Να αποδείξετε ότι $f(x) \le x^2$ για κάθε $x \in [0, 1]$ και στη συνέχεια, να αποδείξετε ότι:
    \[ \dint_0^1 f(x^2) dx < \frac{1}{5} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 24ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%---------------------------------------------------------------
%=========== 25ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η παραγωγίσιμη συνάρτηση $f: \mathbb{R} \to \mathbb{R}$ για την οποία ισχύουν:
\begin{itemize}
    \item $e^x \le f(x) \le (x^2+1)e^x$ για κάθε $x \in \mathbb{R}$.
    \item $f'(x) - f(x) = 2x e^x$ για κάθε $x \in \mathbb{R}$.
\end{itemize}

\begin{erwthma}
    \item Να αποδείξετε ότι $f(0) = 1$, $f'(0) = 1$ και, στη συνέχεια, να αποδείξετε ότι $f(x) = (x^2 + 1)e^x$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και να λύσετε στο $\mathbb{R}$ την ανίσωση: 
    \[ f\left( f(x^2 - 2) - 1 \right) > 1 \]
    \item Να αποδείξετε ότι για κάθε $x > 0$ ισχύει: 
    \[ f(x+2) + f(x) > 2f(x+1) \]
    \item Να υπολογίσετε το ολοκλήρωμα $\displaystyle I = \dint_0^1 f(x) dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 25ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 26ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = (x-1)(e^x - e)$ με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, τα ακρότατα και την κυρτότητα.
    \item Να αποδείξετε ότι η εξίσωση $f(x) = 2e$ έχει ακριβώς δύο ρίζες $x_1, x_2$ για τις οποίες ισχύει $x_1 < -1 < 1 < x_2$.
    \item Να αποδείξετε ότι υπάρχει τουλάχιστον ένα $\xi \in (x_1, x_2)$ τέτοιο ώστε να ισχύει: 
    \[ f'(\xi) + f(\xi) = 2e \]
    \item Να βρείτε την εξίσωση της τέμνουσας ευθείας $(\varepsilon)$ που διέρχεται από τα σημεία $A(0, f(0))$ και $B(1, f(1))$. Στη συνέχεια, αξιοποιώντας την κυρτότητα της $f$ στο διάστημα $[0, 1]$, να αποδείξετε ότι:
    \[ \dint_0^1 f(x) dx < \frac{e-1}{2} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 26ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 27ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = x^3 + x$ με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ αντιστρέφεται και να βρείτε τα κοινά σημεία των γραφικών παραστάσεων $C_f$ και $C_{f^{-1}}$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της $f^{-1}$, τον άξονα $x'x$ και την ευθεία $x = 10$.
    \item Ένα υλικό σημείο $M(x, y)$ κινείται επί της γραφικής παράστασης της $f$ με $x > 0$. Καθώς απομακρύνεται από την αρχή των αξόνων $O(0,0)$, η τετμημένη του αυξάνεται με σταθερό ρυθμό $2 \text{ μονάδες/$\sec$}$. Να βρείτε το ρυθμό μεταβολής του εμβαδού του τριγώνου $OAM$, όπου $A(x, 0)$ η προβολή του $M$ στον άξονα $x'x$, τη χρονική στιγμή $t_0$ που η τεταγμένη του σημείου $M$ είναι $y = 10$.
    \item Να αποδείξετε ότι η $f$ είναι περιττή συνάρτηση και, στη συνέχεια, να λύσετε στο $\mathbb{R}$ την εξίσωση:
    \[ f^{-1}(x^3) + f^{-1}(x - 10) = 0 \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 27ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 28ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω μια συνάρτηση $f: \mathbb{R} \to \mathbb{R}$, δύο φορές παραγωγίσιμη, για την οποία ισχύει:
\[ f''(x) - 2f'(x) + f(x) = 0 \quad \text{για κάθε } x \in \mathbb{R} \]
με $f(0) = 0$ και $f'(0) = 1$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $g(x) = (f'(x) - f(x))e^{-x}$ είναι σταθερή στο $\mathbb{R}$ και στη συνέχεια να αποδείξετε ότι $f(x) = x e^x$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, τα ακρότατα, την κυρτότητα και τα σημεία καμπής.
    \item Να αποδείξετε ότι από το σημείο $A(2, 0)$ άγονται ακριβώς δύο εφαπτομένες προς τη γραφική παράσταση της $f$. Αν $x_1, x_2$ είναι οι τετμημένες των σημείων επαφής, να αποδείξετε ότι το γινόμενο των κλίσεων των δύο εφαπτομένων είναι ίσο με $e^2$.
    \item Να αποδείξετε ότι η εξίσωση $f(x) = x^2 + 1$ έχει μοναδική ρίζα στο $\mathbb{R}$, η οποία ανήκει στο διάστημα $(0, 1)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 28ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 29ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = x \hm x + \syn x$, με $x \in [0, \pi]$.
\begin{erwthma}
    \item Να μελετήσετε τη συνάρτηση $f$ ως προς τη μονοτονία και να βρείτε τα ακρότατά της.
    \item Να αποδείξετε ότι η εξίσωση $f(x) = 0$ έχει ακριβώς μία ρίζα $x_0$, η οποία ανήκει στο διάστημα $\displaystyle\left(\frac{\pi}{2}, \pi\right)$. 
    \item Να αποδείξετε ότι για κάθε $x \in [0, \pi]$ ισχύει:
    \[ f(x) + f(\pi - x) = \pi \hm x \]
    και στη συνέχεια, με τη βοήθεια της αντικατάστασης $u = \pi - x$, να υπολογίσετε το ολοκλήρωμα $\displaystyle \dint_0^\pi f(x) dx$.
    \item Θεωρούμε τη συνάρτηση $H(x) = 2\hm x - x \syn x$, με $x \in [0, \pi]$. Να αποδείξετε ότι η $H$ είναι μια παράγουσα της $f$ στο $[0, \pi]$. Στη συνέχεια, να αποδείξετε ότι υπάρχουν $\xi_1 \in \left(0, \frac{\pi}{2}\right)$ και $\xi_2 \in \left(\frac{\pi}{2}, \pi\right)$ τέτοια ώστε:
    \[ f(\xi_1) + f(\xi_2) = 2 \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 29ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 30ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = e^x - \ln(x+1) - 1$, με $x > -1$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και τα ακρότατα, και να αποδείξετε ότι $e^x \ge \ln(x+1) + 1$ για κάθε $x > -1$.
    \item Δίνεται παραγωγίσιμη συνάρτηση $g: (-1, +\infty) \to \mathbb{R}$ τέτοια ώστε $g(x) \ge f(x)$ για κάθε $x > -1$ και $g(0) = 0$. Να αποδείξετε ότι $g'(0) = 0$ και να βρείτε την εξίσωση της εφαπτομένης της $C_g$ στο σημείο με τετμημένη $x_0 = 0$.
    \item Αν επιπλέον γνωρίζετε ότι υπάρχει το όριο $\displaystyle L = \lim_{x\to 0} \frac{g(x)}{x^2}$ και είναι πραγματικός αριθμός, να υπολογίσετε το όριο $\displaystyle \lim_{x\to 0} \frac{f(x)}{x^2}$ και να αποδείξετε ότι $L \ge 1$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της $f$, τον άξονα $x'x$ και τις ευθείες $x=0$ και $x=e-1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 30ο ΘΕΜΑ Δ ===========

%=========== 31ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω συνάρτηση $f:\mathbb{R}\to\mathbb{R}$ για την οποία ισχύει $f^3(x) + f(x) = x$ για κάθε $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ αντιστρέφεται στο $\mathbb{R}$ και να προσδιορίσετε τον τύπο της αντίστροφης $f^{-1}$.
    \item Να αποδείξετε ότι η $f$ είναι γνησίως αύξουσα, να βρείτε το πρόσημό της και να λύσετε την ανίσωση $f(f(x) - 2) < 0$.
    \item Να αποδείξετε ότι $|f(x) - f(x_0)| \le |x - x_0|$ για κάθε $x, x_0 \in \mathbb{R}$ και να συμπεράνετε ότι η $f$ είναι συνεχής.
    \item Να υπολογίσετε το $\displaystyle\lim_{x\to +\infty} \frac{f(x)}{x}$ και να βρείτε (εφόσον υπάρχουν) τις ασύμπτωτες της γραφικής παράστασης $C_f$ στο $+\infty$.
    \item Θέτοντας $x = f^{-1}(u)$, να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της $f$, τους άξονες $x'x$, $y'y$ και την ευθεία $x=2$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 31ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 32ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε μια δύο φορές παραγωγίσιμη συνάρτηση $f: (0, +\infty) \to \mathbb{R}$ που ικανοποιεί τις συνθήκες:
\[ x f''(x) + f'(x) = -2 \quad \text{για κάθε } x > 0, \quad f(1)=1 \text{ και } f'(1)=-1 \]
\begin{erwthma}
    \item Να προσδιορίσετε τον τύπο της συνάρτησης, αποδεικνύοντας ότι $f(x) = \ln x - 2x + 3$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και τα ακρότατα, και να αποδείξετε ότι η εξίσωση $f(x) = 0$ έχει ακριβώς δύο ρίζες $\rho_1, \rho_2$, τέτοιες ώστε $\displaystyle \rho_1 < \frac{1}{2} < \rho_2$.
    \item Να αποδείξετε ότι η $f$ είναι κοίλη, να βρείτε την εξίσωση της εφαπτομένης της στο σημείο με τετμημένη $x_0 = 1$ και να αποδείξετε ότι $\ln x \le x - 1$ για κάθε $x > 0$.
    \item Αν $\rho_1, \rho_2$ είναι οι ρίζες του ερωτήματος $\Delta 2$, να αποδείξετε την παρακάτω ανισότητα για το ορισμένο ολοκλήρωμα:
    \[ 0 < \dint_{\rho_1}^{\rho_2} f(x) dx < \frac{1}{2}(\rho_2 - \rho_1)(4 - \rho_1 - \rho_2) \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 32ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 33ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = e^x + x - 1$, με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ είναι γνησίως αύξουσα, να βρείτε τη μοναδική ρίζα της εξίσωσης $f(x) = 0$ και να λύσετε την ανίσωση $f(f(\ln x) + 1) > e$.
    \item Να υπολογίσετε τα όρια $\displaystyle\lim_{x\to 0} \frac{f(x) - 2x}{x^2}$ και $\displaystyle\lim_{x\to +\infty} \frac{x f(x) - e^x}{f(x) + e^{x-1}}$.
    \item Αφού αιτιολογήσετε ότι η $f$ αντιστρέφεται στο $\mathbb{R}$, να λύσετε την εξίσωση $f(x) = f^{-1}(x)$ και, θέτοντας $x = f(u)$, να υπολογίσετε το ολοκλήρωμα $\displaystyle \dint_0^e f^{-1}(x) dx$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τις γραφικές παραστάσεις των συναρτήσεων $f, f^{-1}$ και την ευθεία $x+y=e+1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 33ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 34ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Θεωρούμε την συνάρτηση $f(x) = \dfrac{\ln x}{x} - \dfrac{1}{x} + a$, $x>0, a \in \mathbb{R}$.
\begin{erwthma}
    \item Να βρείτε την τιμή του $a \in \mathbb{R}$ ώστε η εφαπτομένη της γραφικής παράστασης $C_f$ στο σημείο με τετμημένη $x_0 = 1$ να διέρχεται από την αρχή των αξόνων $O(0,0)$.
    \item Για $a=3$, να μελετήσετε την $f$ ως προς τη μονοτονία, τα ακρότατα και να αποδείξετε ότι η εξίσωση $f(x) = 0$ έχει ακριβώς μία ρίζα $\rho$, η οποία ανήκει στο διάστημα $(0, 1)$.
    \item Να αποδείξετε ότι η συνάρτηση $\displaystyle F(x) = \frac{1}{2}\ln^2 x - \ln x + 3x$ είναι μια παράγουσα της $f$ στο $(0, +\infty)$ και να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη $C_f$, τον άξονα $x'x$ και τις ευθείες $x=1$ και $x=e^2$.
\item Να αποδείξετε ότι για κάθε $x > 0$ ισχύει $e^2 \ln x \le x + e^2$ και στη συνέχεια να λύσετε στο $(0, +\infty)$ την εξίσωση:
    \[ f(x) + f(x^2 - e^4 + e^2) = \frac{2}{e^2} + 6 \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 34ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 35ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω παραγωγίσιμη συνάρτηση $f:(0, +\infty) \to \mathbb{R}$ η οποία ικανοποιεί τη σχέση $x^3 f'(x) + 2x^2 f(x) = 1$ για κάθε $x > 0$, με $f(1) = 0$.
\begin{erwthma}
    \item Να αποδείξετε ότι $\displaystyle f(x) = \frac{\ln x}{x^2}$ για κάθε $x > 0$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, τα ακρότατα και να βρείτε το σύνολο τιμών της.
    \item Να βρείτε τις ασύμπτωτες της γραφικής παράστασης $C_f$ και να αποδείξετε ότι η εξίσωση $10 \ln x = x^2$ έχει ακριβώς δύο θετικές ρίζες.
    \item Να υπολογίσετε το εμβαδόν $E$ του χωρίου που περικλείεται από τη $C_f$, τον άξονα $x'x$ και την ευθεία $x = e$. Στη συνέχεια, με χρήση της γνωστής ανισότητας $e^u \ge u+1$, να αποδείξετε ότι:
    \[ \dint_1^e e^{f(x)} dx > E + e - 1 \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 35ο ΘΕΜΑ Δ ===========

%=========== 36ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $f(x) = e^{x-1}$ με $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ αντιστρέφεται και να βρείτε την αντίστροφή της, έστω $g(x) = f^{-1}(x)$, καθώς και το πεδίο ορισμού της.
    \item Να αποδείξετε ότι $g(x) \le x \le f(x)$ για κάθε $x>0$. Στη συνέχεια να βρείτε το κοινό σημείο των γραφικών παραστάσεων $C_f, C_g$ και να δείξετε ότι σε αυτό δέχονται κοινή εφαπτομένη.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τις $C_f, C_g$ και τις ευθείες $x=1$ και $x=e$.
    \item Να υπολογίσετε το όριο $\displaystyle \lim_{x\to 1} \frac{f(x) - g(x)}{(x-1)^2}$ και στη συνέχεια να αποδείξετε ότι $\displaystyle \dint_1^2 e^{x^2-1} dx > \frac{7}{3}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 36ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 37ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
H συνεχής συνάρτηση $f:\mathbb{R}\to\mathbb{R}$ ικανοποιεί τη σχέση $(f(x) - x)(f(x) + x) = x^2 + 1$ για κάθε $x \in \mathbb{R}$ και διέρχεται από το σημείο $A(0, 1)$.
\begin{erwthma}
    \item Να αποδείξετε ότι $f(x) = \sqrt{2x^2 + 1}$ για κάθε $x \in \mathbb{R}$.
    \item Να αποδείξετε ότι υπάρχουν $\xi_1 \in (-1, 0)$ και $\xi_2 \in (0, 1)$ τέτοια ώστε $f'(\xi_1) + f'(\xi_2) = 0$.
    \item Να μελετήσετε την $f$ ως προς την κυρτότητα.
    \item Αν $a < \beta < \gamma$ είναι διαδοχικοί όροι αριθμητικής προόδου, να αποδείξετε ότι $f(a) + f(\gamma) > 2f(\beta)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 37ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 38ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = \begin{cases} x^2 \hm\left(\frac{1}{x}\right), & x \ne 0 \\ 0, & x = 0 \end{cases}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ είναι συνεχής και παραγωγίσιμη στο $x_0 = 0$ με $f'(0) = 0$.
    \item Να βρείτε την παράγωγο συνάρτηση $f'(x)$ για κάθε $x \in \mathbb{R}$.
    \item Να αποδείξετε ότι το όριο $\displaystyle \lim_{x\to 0} \cos\left(\frac{1}{x}\right)$ δεν υπάρχει, και να συμπεράνετε ότι η $f'$ δεν είναι συνεχής στο $x_0 = 0$.
    \item Να υπολογίσετε την τιμή του ολοκληρώματος $\displaystyle \dint_{1/\pi}^{2/\pi} f'(x) dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 38ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 39ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω παραγωγίσιμη συνάρτηση $f: (0,+\infty) \to \mathbb{R}$ με $f(1)=1$ η οποία ικανοποιεί τη σχέση:
\[ x f'(x) - f(x) = x^2 e^{-x} \]
για κάθε $x>0$.
\begin{erwthma}
    \item Να αποδείξετε ότι $\displaystyle f(x) = x \left(1 + \frac{1}{e} - e^{-x}\right)$ για κάθε $x>0$.
    \item Να αποδείξετε ότι η γραφική παράσταση της $f$ δέχεται πλάγια ασύμπτωτη στο $+\infty$, την οποία και να βρείτε (έστω $\varepsilon$).
    \item Να μελετήσετε την $f$ ως προς την κυρτότητα, να βρείτε το σημείο καμπής της και να αποδείξετε ότι υπάρχει μοναδική εφαπτομένη της $C_f$ που είναι παράλληλη στην $(\varepsilon)$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη $C_f$, την ασύμπτωτη $(\varepsilon)$ και τις κατακόρυφες ευθείες $x=1$ και $x=2$. Στη συνέχεια να αποδείξετε ότι:
    \[ \dint_1^2 f(x) dx > \frac{3e+1}{2e} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 39ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 40ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω παραγωγίσιμη συνάρτηση $f:\mathbb{R} \to \mathbb{R}$ η οποία ικανοποιεί τη σχέση $f'(x) - f(x) = e^x$ για κάθε πραγματικό αριθμό $x$, με $f(0)=0$.
\begin{erwthma}
    \item Να αποδείξετε ότι $f(x) = x e^x$ για κάθε $x \in \mathbb{R}$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, τα ακρότατα και να βρείτε το σύνολο τιμών της.
    \item Να αποδείξετε ότι από την αρχή των αξόνων $O(0,0)$ άγεται ακριβώς μία εφαπτομένη προς τη γραφική παράσταση $C_f$, της οποίας να βρείτε την εξίσωση.
    \item Να λύσετε την εξίσωση $\displaystyle (x^2+1) e^{x^2-x} = x+1$ στο $\mathbb{R}$ και στη συνέχεια να αποδείξετε ότι $x e^x > (x+1)\ln(x+1)$ για κάθε $x>0$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 40ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 41ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η συνάρτηση $\displaystyle f(x) = \ln(e^x - x), \ x \in \mathbb{R}$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, να βρείτε το ολικό της ακρότατο και στη συνέχεια να αποδείξετε ότι $e^x \ge x+1$ για κάθε $x \in \mathbb{R}$.
    \item Να αποδείξετε ότι η γραφική παράσταση της $f$ έχει ακριβώς δύο σημεία καμπής $M_1(\rho_1, f(\rho_1))$ και $M_2(\rho_2, f(\rho_2))$ με $\rho_1 < 0$ και $\rho_2 > 1$. Στη συνέχεια να αιτιολογήσετε γιατί η $f$ είναι κυρτή στο διάστημα $[0, 1]$.
    \item Να αποδείξετε ότι η ευθεία $y = x$ είναι πλάγια ασύμπτωτη της $C_f$ στο $+\infty$ και ότι η εξίσωση $f(x) = 2026$ έχει ακριβώς δύο πραγματικές ρίζες, μία θετική και μία αρνητική.
    \item Να υπολογίσετε το όριο $\displaystyle \lim_{x\to 0} \frac{f(x)}{x^2}$ και, με βάση την κυρτότητα της $f$, να αποδείξετε ότι:
    \[ \dint_0^1 f(x) dx < \frac{\ln(e-1)}{2} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 41ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 42ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η συνάρτηση $\displaystyle f(x) = e^{x-1} + \ln x + x - 2$ με $x>0$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και να βρείτε το σύνολο τιμών της. Στη συνέχεια, να λύσετε την εξίσωση $f(x) = 0$.
    \item Να μελετήσετε την $f$ ως προς την κυρτότητα, να αποδείξετε ότι παρουσιάζει ακριβώς ένα σημείο καμπής και να βρείτε την εξίσωση της εφαπτομένης της $C_f$ σε αυτό.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της $f$, την εφαπτομένη της στο σημείο καμπής της και την ευθεία $x=e$.
    \item Να λύσετε την εξίσωση $e^{\ln x - 1} + \ln(\ln x) + \ln x = 2$ και να αποδείξετε ότι η εξίσωση $x f(x) = e^2 - 1$ έχει ακριβώς μία ρίζα στο διάστημα $(1, e)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 42ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 43ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Μία σφαίρα πάγου αρχίζει να λιώνει. Ο ρυθμός μείωσης του όγκου της είναι ανάλογος του εμβαδού της επιφάνειάς της, δηλαδή $V'(t) = -k E(t)$ για κάθε $t \in [0, t_{ολ}]$ με σταθερά αναλογίας $k > 0$, όπου $t_{ολ}$ ο χρόνος ολικής τήξης της σφαίρας. Θεωρούμε τον όγκο $V(t)$, το εμβαδόν επιφάνειας $E(t)$ και την ακτίνα $R(t)$ ως παραγωγίσιμες συναρτήσεις του χρόνου $t$ (σε ώρες).
\begin{erwthma}
    \item Να αποδείξετε ότι ο ρυθμός μεταβολής της ακτίνας είναι σταθερός διαχρονικά, δηλαδή $R'(t) = -k$.
    \item Αν ο αρχικός όγκος της σφαίρας είναι $V_0$ και μετά από $2$ ώρες έχει μειωθεί στο $\frac{V_0}{8}$, να βρείτε το χρόνο ολικής τήξης $t_{ολ}$.
    \item Να αποδείξετε ότι ο ρυθμός μεταβολής $E'(t)$ του εμβαδού της επιφάνειας της σφαίρας είναι γνησίως αύξουσα συνάρτηση του χρόνου στο διάστημα $(0, t_{ολ})$.
    \item Αν θεωρήσουμε δεδομένο ότι αρχικά ο όγκος είναι $V_0 = 8\pi$, να αποδείξετε ότι υπάρχει ακριβώς μία χρονική στιγμή $t_0 \in (0, t_{ολ})$ κατά την οποία ο υπολειπόμενος όγκος του πάγου ισούται με $1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 43ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 44ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση:
\[ f(x) = \begin{cases} 
\dfrac{\ln x}{x-1}, & 0 < x \neq 1 \\[2ex]
1, & x = 1 
\end{cases} \]
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι συνεχής στο $x_0 = 1$ και να μελετήσετε τη μονοτονία της στο $(0, +\infty)$.
    \item Να αποδείξετε ότι η $f$ είναι παραγωγίσιμη στο $x_0 = 1$ με $f'(1) = -\frac{1}{2}$ και να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο $A(1, f(1))$.
    \item Να βρείτε το σύνολο τιμών της $f$ και να αποδείξετε ότι η εξίσωση $2\ln x = x - 1$ έχει ακριβώς μία ρίζα στο διάστημα $(1, e^2)$.
    \item Χρησιμοποιώντας τη μονοτονία της $f$, να αποδείξετε ότι $\ln x \le x - 1$ για κάθε $x > 0$. Στη συνέχεια, να υπολογίσετε το όριο $\displaystyle \lim_{x\to 0^+} \left( f(x) \hm x \right)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 44ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 45ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = e^x - \ln(x+1) - 1$, ορισμένη στο $(-1, +\infty)$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και τα ακρότατα, και να αποδείξετε ότι $e^x - 1 \ge \ln(x+1)$ για κάθε $x > -1$.
    \item Να βρείτε το σύνολο τιμών της $f$ και το πλήθος των ριζών της εξίσωσης $2026 f(x) = 2025$.
    \item Να αποδείξετε ότι η $f$ είναι κυρτή στο $(-1, +\infty)$ και να λύσετε στο διάστημα $[0, +\infty)$ την εξίσωση $f(x) + f(3x) = 2f(2x)$.
    \item Να αιτιολογήσετε ότι η $f$ αντιστρέφεται στο διάστημα $[0, +\infty)$. Αν $g$ είναι η αντίστροφη συνάρτηση της $f$ στο διάστημα αυτό, να υπολογίσετε το ολοκλήρωμα $\displaystyle \dint_0^{e-1-\ln 2} g(x) dx$ και στη συνέχεια να βρείτε το όριο:
    \[ \lim_{x\to 0^+} \frac{x^2 - f(x)}{x f(x)} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 45ο ΘΕΜΑ Δ ===========

%=========== 46ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η παραγωγίσιμη συνάρτηση $f : \mathbb{R} \to \mathbb{R}$ για την οποία ισχύει $e^{f(x)} + f(x) = x + 1$ για κάθε $x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι γνησίως αύξουσα και κοίλη στο $\mathbb{R}$.
    \item Να βρείτε το σύνολο τιμών της $f$ και να λύσετε την ανίσωση $\displaystyle f(f(x^2 - 4)) < 0$.
    \item Να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο με τετμημένη $x_0 = 0$ και να υπολογίσετε το όριο:
    \[ \lim_{x \to 0} \frac{2f(x) - x}{x^2} \]
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη γραφική παράσταση της $f$, την παραπάνω εφαπτομένη και την κατακόρυφη ευθεία $x = e$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 46ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 47ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω ότι η $g: (0, +\infty) \to \mathbb{R}$ ικανοποιεί τη σχέση $g(xy) = g(x) + g(y)$ για όλα τα $x, y > 0$. Δίνεται επιπλέον ότι η $g$ είναι παραγωγίσιμη στο $x_0 = 1$ με $g'(1) = 1$.
\begin{erwthma}
    \item Να αποδείξετε ότι $g(1) = 0$ και στη συνέχεια, χρησιμοποιώντας τον ορισμό της παραγώγου, να αποδείξετε ότι η $g$ είναι παραγωγίσιμη σε κάθε $x > 0$ με $\displaystyle g'(x) = \frac{1}{x}$.
    \item Να αποδείξετε ότι $g(x) = \ln x$ και στη συνέχεια να μελετήσετε ως προς τη μονοτονία και τα ακρότατα τη συνάρτηση $\displaystyle f(x) = \frac{g(x)}{x}, \; x > 0$.
    \item Να αποδείξετε ότι για κάθε $a > e$, η εξίσωση $x^a = a^x$ έχει ακριβώς δύο θετικές ρίζες. Στη συνέχεια να λύσετε στο $(0, +\infty)$ την εξίσωση $x^e = e^x$.
    \item Να αποδείξετε ότι υπάρχει μοναδικό $\xi \in (1, e)$ τέτοιο ώστε $\displaystyle f(\xi) = \frac{1}{2} \dint_1^e f(x) dx$, και στη συνέχεια να αποδείξετε ότι $\xi < \sqrt{e}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 47ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 48ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνονται οι συναρτήσεις $f,g\mathbb{R}\to\mathbb{R}$ για τις οποίες ισχύουν
\begin{itemize}
\item $g(0)=0$ και $g(1)=\dfrac{\pi}{4}$
\item $f'(x)=g(x)-\dfrac{x}{x^2+1}$ και $g'(x)=\dfrac{1}{x^2+1}$, για κάθε $x\in\mathbb{R}$
\end{itemize}
\begin{erwthma}
    \item Να αποδείξετε ότι η $f$ είναι κυρτή.
\item Να αποδείξετε ότι \[f(x)-f(\hm{x})<\left(x-\hm{x}\right)f'(x)\]
για κάθε $x>0$.
\item Να λύσετε την ανίσωση \[f\left(x^2+1\right)-f(2)<f\left(\frac{1}{x^2+1}\right)-f\left(\frac{1}{2}\right)\]
\item Αν επιπλέον ισχύει $\dint_{0}^{1}{xf(x)\d x}>\frac{\pi}{4}$, τότε να αποδείξετε ότι υπάρχει τουλάχιστον ένα $x_0\in(0,1)$ τέτοιο ώστε
\[x_0^3f(x_0)+x_0f(x_0)=1\]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 48ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 49ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω η παραγωγίσιμη συνάρτηση $f: \mathbb{R} \to \mathbb{R}$ η οποία ικανοποιεί τη σχέση:
\[ (x^2 + 1) f'(x) + 2x f(x) = 2x \]
για κάθε $x \in \mathbb{R}$, με $f(0) = 0$.
\begin{erwthma}
    \item Να αποδείξετε ότι ο τύπος της συνάρτησης $f$ είναι $\displaystyle f(x) = \frac{x^2}{x^2+1}, \; x \in \mathbb{R}$.
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία, τα τοπικά ακρότατα, την κυρτότητα και τα σημεία καμπής.
    \item Να αποδείξετε ότι υπάρχουν ακριβώς δύο εφαπτόμενες της γραφικής παράστασης της $f$ οι οποίες διέρχονται από το σημείο $\displaystyle M\left(0, \frac{1}{2}\right)$.
    \item Να αποδείξετε ότι $\displaystyle \frac{1}{6} < \dint_0^1 f(x) dx < \frac{1}{3}$, και στη συνέχεια να δείξετε ότι υπάρχει μοναδικό $\xi \in (0, 1)$ τέτοιο ώστε $\displaystyle f(\xi) = \dint_0^1 f(x)dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 49ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 50ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η παραγωγίσιμη συνάρτηση $f:[-2,2]\to\mathbb{R}$ για την οποία ισχύουν:
\begin{itemize}
    \item $\ln{\left(f(0)-1\right)}+f(0)-2=0$
    \item $f(x)\cdot f'(x)=-x$ για κάθε $x\in(-2,2)$
\end{itemize}
\begin{erwthma}
    \item Έστω η συνάρτηση $g(y) = \ln(y-1) + y - 2, \; y > 1$. Να μελετήσετε την $g$ ως προς τη μονοτονία και να αποδείξετε ότι $f(0)=2$.
    \item Να αποδείξετε ότι ο τύπος της $f$ είναι $\displaystyle f(x)=\sqrt{4-x^2}$ για κάθε $x\in[-2,2]$.
    \item Στο χωρίο που περικλείεται από τη γραφική παράσταση της $f$ και τον άξονα $x'x$, εγγράφουμε ορθογώνιο $K\Lambda MN$ με κορυφές $K(-x, 0), \Lambda(x, 0)$ στον άξονα $x'x$ και $M(x, f(x)), N(-x, f(-x))$ πάνω στη $C_f$, όπου $x \in (0, 2)$. Να βρείτε την τιμή του $x$ για την οποία το εμβαδόν του ορθογωνίου γίνεται μέγιστο.
    \item Να υπολογίσετε το ολοκλήρωμα $\displaystyle \dint_{-2}^2 x^3 f(x) dx$ και στη συνέχεια το όριο:
    \[ \lim_{x \to 0} \frac{f(x) - 2}{x \eta\mu x} \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 50ο ΘΕΜΑ Δ ===========

%=========== 51ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνονται οι συναρτήσεις $\displaystyle f(x) = e^x - 1, \; x \in \mathbb{R}$ και $\displaystyle g(x) = \ln(x+1), \; x > -1$.
\begin{erwthma}
    \item Να αποδείξετε ότι για κάθε $x > -1$ ισχύει $g(x) \le x \le f(x)$, και ότι οι γραφικές παραστάσεις των $f$ και $g$ έχουν ακριβώς ένα κοινό σημείο, στο οποίο δέχονται κοινή εφαπτομένη.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τις $C_f, C_g$ και την ευθεία $x = 1$.
    \item Θεωρούμε τη συνάρτηση:
    \[ h(x) = \begin{cases} 
        \displaystyle \frac{f(x)}{g(x)}, & x \in (-1, 0) \cup (0, +\infty) \\[2ex]
        1, & x = 0 
    \end{cases} \]
    Να αποδείξετε ότι η $h$ είναι συνεχής στο $x_0 = 0$ και στη συνέχεια ότι είναι παραγωγίσιμη στο σημείο αυτό, με $h'(0) = 1$.
    \item Αξιοποιώντας τον ορισμό της παραγώγου από το ερώτημα Δ3, να υπολογίσετε το όριο:
    \[ \lim_{x \to 0} \left( \frac{1}{g(x)} - \frac{1}{f(x)} \right) \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 51ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 52ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η πολυωνυμική συνάρτηση $\displaystyle P(x) = x^3 - 3x^2 - 9x + \mu, \; x \in \mathbb{R}$, όπου $\mu \in \mathbb{R}$ σταθερά.
\begin{erwthma}
    \item Να αποδείξετε ότι για κάθε $\mu \in \mathbb{R}$ η συνάρτηση $P$ παρουσιάζει ακριβώς ένα τοπικό μέγιστο, ένα τοπικό ελάχιστο και ένα σημείο καμπής.
    \item Να βρείτε την τιμή του $\mu$ ώστε το σημείο καμπής της γραφικής παράστασης της $P$ να βρίσκεται πάνω στον άξονα $x'x$.
\end{erwthma}
Στα παρακάτω ερωτήματα, θεωρήστε ότι $\mu = 11$:
\begin{erwthma}[resume]
    \item \begin{alist}
        \item Να αποδείξετε ότι για κάθε $x \in \mathbb{R}$ ισχύει $P(2-x) = -P(x)$ και να ερμηνεύσετε γεωμετρικά το αποτέλεσμα ως προς τη γραφική παράσταση της $P$.
        \item Να βρείτε το πλήθος των πραγματικών ριζών της εξίσωσης $P(x) = 0$.
    \end{alist}
    \item Να υπολογίσετε το ολοκλήρωμα:
    \[ \dint_0^2 \left(P(x)\right)^{2025} dx \]
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 52ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 53ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = x + \eta\mu x, \; x \in \mathbb{R}$.
\begin{erwthma}
    \item Να αποδείξετε ότι η συνάρτηση $f$ είναι γνησίως αύξουσα στο $\mathbb{R}$. Στη συνέχεια, να λύσετε την ανίσωση $\displaystyle f(x^2 - x + \pi) > \pi$.
    \item Να μελετήσετε την $f$ ως προς την κυρτότητα στο διάστημα $[0, 2\pi]$, να βρείτε το σημείο καμπής $A$ της $C_f$ στο διάστημα αυτό, και να γράψετε την εξίσωση της εφαπτομένης της στο $A$.
    \item Να αποδείξετε ότι για κάθε $x \in \mathbb{R}$ ισχύει $f(2\pi - x) + f(x) = 2\pi$ και να ερμηνεύσετε γεωμετρικά το αποτέλεσμα ως προς τη γραφική παράσταση της $f$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη $C_f$, την εφαπτομένη της στο σημείο $A$ και τις κατακόρυφες ευθείες $x=0$ και $x=2\pi$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 53ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 54ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = (x^2+1)e^{-x}, \; x \in \mathbb{R}$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και να αποδείξετε ότι το σύνολο τιμών της είναι το $(0, +\infty)$.
    \item Να αποδείξετε ότι η γραφική παράσταση της $f$ έχει ακριβώς δύο σημεία καμπής, τα οποία και να βρείτε. Στη συνέχεια, να γράψετε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο καμπής με τη μικρότερη τετμημένη.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που περικλείεται από τη $C_f$, τους άξονες $x'x, y'y$ και την ευθεία $x=1$.
    \item Να υπολογίσετε το όριο:
    \[ L = \lim_{x\to +\infty} \frac{\ln(f(x))}{x} \]
    και στη συνέχεια να αποδείξετε ότι η εξίσωση $\displaystyle f(x) = e^{L x} + \frac{x}{4}$ έχει ακριβώς μία ρίζα στο διάστημα $(0, 1)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 54ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 55ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $g(x) = e^x + e^{-x}, \; x \ge 0$.
\begin{erwthma}
    \item Να μελετήσετε την $g$ ως προς τη μονοτονία. Στη συνέχεια, να αποδείξετε ότι για κάθε $x \ge 1$, η εξίσωση $g(y) = 2x$ έχει ακριβώς μία λύση $y \in [0, +\infty)$.
    \item Θεωρούμε τη συνάρτηση $f: [1, +\infty) \to \mathbb{R}$ με $f(x) \ge 0$, η οποία για κάθε $x \ge 1$ ικανοποιεί τη σχέση:
    \[ e^{f(x)} + e^{-f(x)} = 2x \]
    Να αποδείξετε ότι η $f$ είναι γνησίως αύξουσα.
    \item Να αποδείξετε ότι ο τύπος της $f$ είναι $\displaystyle f(x) = \ln\left(x + \sqrt{x^2-1}\right)$ για κάθε $x \ge 1$ και να ορίσετε την αντίστροφη συνάρτηση $f^{-1}$.
    \item Να υπολογίσετε το ολοκλήρωμα:
    \[ I = \dint_1^{\frac{e^2+1}{2e}} f(x) dx \]
    αξιοποιώντας την αντίστροφη συνάρτηση $f^{-1}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 55ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 56ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = x\ln x - x + 1, \; x > 0$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία. Να αποδείξετε ότι $f(x) \ge 0$ για κάθε $x > 0$ και να συναγάγετε την ανίσωση:
    \[ x\ln x \ge x - 1, \quad \forall x > 0 \]
    \item Να αποδείξετε ότι η $C_f$ είναι κυρτή σε όλο το $(0,+\infty)$. Να βρείτε την εξίσωση της εφαπτομένης της $C_f$ στο ελάχιστο της $f$ και να αποδείξετε ότι η εφαπτομένη αυτή τέμνει τη $C_f$ μόνο σε εκείνο το σημείο.
    \item Να αποδείξετε ότι $\ln x \ge 1 - \dfrac{1}{x}$ για κάθε $x > 0$. Στη συνέχεια, να βρείτε το πλήθος των λύσεων της εξίσωσης $\ln x = 1 - \dfrac{1}{x}$.
    \item Να υπολογίσετε το ολοκλήρωμα $\displaystyle\dint_1^2 f(x)\, dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 56ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 57ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Ο πληθυσμός βακτηρίων (σε χιλιάδες) τη χρονική στιγμή $t \ge 0$ (σε ώρες) περιγράφεται από τη συνάρτηση:
\[ P(t) = \begin{cases} 1000 e^{t/2} & 0 \le t \le 10 \\ A e^{-t/2} & t > 10 \end{cases} \]
όπου $A > 0$ σταθερά.
\begin{erwthma}
    \item Να βρείτε την τιμή της σταθεράς $A$ ώστε η $P$ να είναι συνεχής στο $t = 10$.
    \item Να εξετάσετε αν η $P$ είναι παραγωγίσιμη στο $t = 10$ και να ερμηνεύσετε γεωμετρικά το αποτέλεσμα.
    \item Να βρείτε τη χρονική στιγμή $t_0 > 10$ κατά την οποία ο πληθυσμός επιστρέφει στα 1000 (χιλιάδες βακτήρια). Εκφράστε το αποτέλεσμα ως $t_0 = 10 + k$ και βρείτε το $k$.
    \item Να υπολογίσετε το ολοκλήρωμα $\displaystyle\dint_0^{10} P(t)\,dt$, το οποίο εκφράζει τη «συνολική έκθεση» στα βακτήρια κατά τη φάση ανάπτυξης. Χωρίς υπολογισμό, να συγκρίνετε αυτό με το $\displaystyle\dint_{10}^{t_0} P(t)\,dt$ αξιοποιώντας μια κατάλληλη ανίσωση για τις τιμές του $P$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 57ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 58ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω $f: \mathbb{R} \to \mathbb{R}$ παραγωγίσιμη συνάρτηση με $f'(0) = 1$, που ικανοποιεί τη σχέση:
\[ f(x+y) = f(x) \cdot f(y), \quad \forall x, y \in \mathbb{R} \]
\begin{erwthma}
    \item Να αποδείξετε ότι $f(0) = 1$, ότι $f(x) > 0$ για κάθε $x \in \mathbb{R}$, και ότι η $f$ είναι γνησίως αύξουσα.
    \item Χρησιμοποιώντας τον ορισμό της παραγώγου, να αποδείξετε ότι $f'(x) = f(x)$ για κάθε $x \in \mathbb{R}$.
    \item Δεδομένου ότι $f(1) = e$, να υπολογίσετε το όριο $\displaystyle\lim_{x \to 0} \frac{f(3x) - f(x)}{f(x) - f(-x)}$.
    \item Να λύσετε την ανίσωση $f(x^2 - x) > f(x)$ και να υπολογίσετε το $\displaystyle\dint_0^1 f(x)\,dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 58ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 59ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Έστω $F: \mathbb{R} \to \mathbb{R}$ παραγωγίσιμη συνάρτηση με $F(0) = 1$ και $F'(x) = f(x)$, τέτοια ώστε $F(x) \cdot f(x) = x$ για κάθε $x \in \mathbb{R}$.
\begin{erwthma}
    \item Παραγωγίζοντας τη συνάρτηση $\tfrac{1}{2}F^2(x)$ και χρησιμοποιώντας τη δεδομένη σχέση, να αποδείξετε ότι $F(x) = \sqrt{x^2+1}$ για κάθε $x \in \mathbb{R}$.
    \item Να βρείτε τον τύπο της $f(x)$. Να εξετάσετε αν η $f$ είναι άρτια ή περιττή και να βρείτε τις οριζόντιες ασύμπτωτες της $C_f$.
    \item Να βρείτε τις πλάγιες ασύμπτωτες της γραφικής παράστασης $C_F$ καθώς $x \to +\infty$ και $x \to -\infty$.
    \item Αξιοποιώντας τη σχέση $F(x) \cdot f(x) = x$, να υπολογίσετε το ολοκλήρωμα $\displaystyle\dint_0^1 x \cdot F(x) \cdot f(x)\,dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 59ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------
%=========== 60ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνεται η συνάρτηση $\displaystyle f(x) = \frac{\ln x}{x}, \; x > 0$.
\begin{erwthma}
    \item Να μελετήσετε την $f$ ως προς τη μονοτονία και τα ακρότατα. Στη συνέχεια, να αποδείξετε ότι $\dfrac{\ln x}{x} \le \dfrac{1}{e}$ για κάθε $x > 0$.
    \item Να αποδείξετε ότι $e \cdot \ln x \le x$ για κάθε $x > 0$, με ισότητα μόνο για $x = e$, και να συναγάγετε ότι $e^\pi > \pi^e$.
    \item Να βρείτε, αναλόγως της τιμής της σταθεράς $k \in \mathbb{R}$, το πλήθος των λύσεων της εξίσωσης $\dfrac{\ln x}{x} = k$.
    \item Να υπολογίσετε το εμβαδόν του χωρίου που ορίζεται από τη $C_f$, τον άξονα $x'x$ και τις ευθείες $x = 1$ και $x = e$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 60ο ΘΕΜΑ Δ ===========

%===============================================================
\subsection*{Ασκήσεις Οπτικής Επίλυσης}

%=========== 61ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο παρακάτω σχήμα δίνεται η γραφική παράσταση της \textbf{παραγώγου} $f'$ μιας συνεχούς συνάρτησης $f$ ορισμένης στο $[-1, 7]$. Η $C_{f'}$ αποτελείται από τρία μέρη: δύο πάνω και ένα κάτω. Η $f'$ μηδενίζεται στα $x = 1$ και $x = 5$, ενώ στο $x=3$ εμφανίζει ελάχιστη τιμή. Τα επιμέρους εμβαδά είναι: $E_1 = 2$, $E_2 = 5$, $E_3 = 4$.

\begin{center}
\begin{tikzpicture}[scale=0.75]
  % grid
  \draw[help lines, gray!30] (-2,-4) grid (8,4);
  % axes
  \draw[-latex] (-2.3,0) -- (8.3,0) node[right] {$x$};
  \draw[-latex] (0,-4.3) -- (0,4.3) node[above] {$y$};
  % ticks
  \foreach \x in {-1,1,2,3,4,5,6,7}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-3,-2,-1,1,2,3}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % f' curve: positive lobe, negative lobe, positive lobe
  % Lobe 1: [-1,1], above, peak at 0
  \fill[secondcolor!20, opacity=0.5] (-1,0) .. controls (-0.5,3) and (0.5,3) .. (1,0) -- cycle;
  \draw[very thick, secondcolor] (-1,0) .. controls (-0.5,3) and (0.5,3) .. (1,0);
  % Lobe 2: [1,5], below, valley at 3
  \fill[red!15, opacity=0.5] (1,0) .. controls (1.5,-3.5) and (4.5,-3.5) .. (5,0) -- cycle;
  \draw[very thick, secondcolor] (1,0) .. controls (1.5,-3.5) and (4.5,-3.5) .. (5,0);
  % Lobe 3: [5,7], above, peak at 6
  \fill[secondcolor!20, opacity=0.5] (5,0) .. controls (5.5,3) and (6.5,3) .. (7,0) -- cycle;
  \draw[very thick, secondcolor] (5,0) .. controls (5.5,3) and (6.5,3) .. (7,0);
  % labels
  \node at (0,1.5) {$E_1 = 2$};
  \node at (3,-2) {$E_2 = 5$};
  \node at (6,1.5) {$E_3 = 4$};
  % key points
  \filldraw[secondcolor] (-1,0) circle (2pt);
  \filldraw[secondcolor] (1,0) circle (2pt);
  \filldraw[secondcolor] (5,0) circle (2pt);
  \filldraw[secondcolor] (7,0) circle (2pt);
  \node[secondcolor] at (7.5,2) {$C_{f'}$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να κατασκευάσετε πίνακα μεταβολών της $f$ στο $[-1, 7]$.
  \item Αν $f(-1) = 0$, να υπολογίσετε τις τιμές $f(1)$, $f(5)$, $f(7)$. Ποιο είναι το ολικό μέγιστο και ποιο το ολικό ελάχιστο;
  \item Να αποδείξετε ότι η εξίσωση $f(x) = 0$ έχει τουλάχιστον μία ρίζα στο διάστημα $(1, 5)$.
  \item Να βρείτε το ακριβές πλήθος των ριζών της εξίσωσης $f(x) = 0$ στο διάστημα $[-1, 7]$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 61ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 62ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο παρακάτω σχήμα δίνονται \textbf{ταυτόχρονα} οι γραφικές παραστάσεις δύο συναρτήσεων: μιας μπλε καμπύλης και μιας κόκκινης καμπύλης στο $[-1, 4]$. Μία από αυτές είναι η $f$ και η άλλη η $f'$.

\begin{center}
\begin{tikzpicture}[scale=0.85]
  % grid
  \draw[help lines, gray!30] (-2,-3) grid (5,5);
  % axes
  \draw[-latex] (-2.3,0) -- (5.3,0) node[right] {$x$};
  \draw[-latex] (0,-3.3) -- (0,5.3) node[above] {$y$};
  % ticks
  \foreach \x in {-1,1,2,3,4}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-2,-1,1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % Blue curve: f(x) = x^3/3 - 2x + 3 (local max near x=-... wait, domain [-1,4])
  % Let's use: f(x) = -x^2 + 4x - 1 (parabola, max at x=2, f(2)=3)
  \draw[very thick, maincolor, domain=-.4:4, samples=60] plot (\x, {-(\x)*(\x) + 4*(\x) - 1});
  % Red curve: f'(x) = -2x + 4
  \draw[very thick, secondcolor, domain=-.4:3.2, samples=60] plot (\x, {-2*(\x) + 4});
  % shaded area between them from 1 to 3
  \fill[yellow!20, opacity=0.5, domain=1:3, samples=40]
    (1,{-(1)*(1)+4*(1)-1}) -- plot (\x, {-(\x)*(\x)+4*(\x)-1}) -- (3,{-(3)*(3)+4*(3)-1})
    -- plot[domain=3:1] (\x, {-2*(\x)+4}) -- cycle;
  % labels
  \node[maincolor] at (3.5,4) {μπλε};
  \node[secondcolor] at (3.5,-1.5) {κόκκινη};
  % key points
  \filldraw[maincolor] (2,3) circle (2pt) node[above right,font=\footnotesize] {$(2,3)$};
  \filldraw[secondcolor] (2,0) circle (2pt) node[below right,font=\footnotesize] {$(2,0)$};
  \filldraw[maincolor] (0,-1) circle (2pt);
  \filldraw[secondcolor] (0,4) circle (2pt);
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να εξηγήσετε τεκμηριωμένα ποια καμπύλη αντιστοιχεί στη $f$ και ποια στην $f'$. (Υπόδειξη: παρατηρήστε τις μηδενικές τιμές και τα ακρότατα.)
  \item Να γράψετε την εξίσωση της εφαπτομένης της $C_f$ στο σημείο με τετμημένη $x_0 = 0$, χρησιμοποιώντας τιμές που διαβάζετε από το γράφημα.
  \item Να υπολογίσετε γεωμετρικά ή αλγεβρικά το εμβαδόν του σκιασμένου (κίτρινου) χωρίου μεταξύ $C_f$ και $C_{f'}$ στο $[1, 3]$.
  \item Αν $g(x) = f(x) - f'(x)$, να αποδείξετε ότι η εξίσωση $g(x) = 0$ έχει \textbf{ακριβώς μία} ρίζα στο $(-1, 4)$. (Υπόδειξη: Θεώρημα Bolzano για ύπαρξη, μονοτονία $g$ για μοναδικότητα.)
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 62ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 63ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο παρακάτω σχήμα δίνεται η γραφική παράσταση της \textbf{δεύτερης παραγώγου} $f''$ μιας δύο φορές παραγωγίσιμης συνάρτησης $f$ ορισμένης στο $[-2, 4]$. Η $f''$ είναι τμηματικά σταθερή:
\[ f''(x) = \begin{cases} 2, & x \in [-2, 1) \\ -1, & x \in (1, 4] \end{cases} \]
Δίνεται επιπλέον ότι $f'(1) = 0$ και $f(1) = 3$.

\begin{center}
\begin{tikzpicture}[scale=0.85]
  % grid
  \draw[help lines, gray!30] (-3,-2) grid (5,3);
  % axes
  \draw[-latex] (-3.3,0) -- (5.3,0) node[right] {$x$};
  \draw[-latex] (0,-2.3) -- (0,3.3) node[above] {$y$};
  % ticks
  \foreach \x in {-2,-1,1,2,3,4}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-1,1,2}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % f'' step function
  \draw[very thick, secondcolor] (-2,2) -- (1,2);
  \draw[very thick, secondcolor] (1,-1) -- (4,-1);
  % discontinuity at x=1
  \filldraw[secondcolor] (-2,2) circle (2pt);
  \draw[secondcolor, fill=white] (1,2) circle (2pt);
  \filldraw[secondcolor] (1,-1) circle (2pt);
  \filldraw[secondcolor] (4,-1) circle (2pt);
  % dashed line at x=1
  \draw[dashed, gray] (1,-1) -- (1,2);
  % labels
  \node[secondcolor] at (-0.5,2.5) {$f''(x) = 2$};
  \node[secondcolor] at (2.5,-1.5) {$f''(x) = -1$};
  \node[secondcolor] at (4.5,1) {$C_{f''}$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να μελετήσετε την κυρτότητα της $f$ στο $[-2, 4]$ και να βρείτε πιθανό σημείο καμπής.
  \item Ολοκληρώνοντας τη $f''(x)$ σε κάθε τμήμα και χρησιμοποιώντας τη συνθήκη $f'(1) = 0$, να βρείτε τον τύπο της $f'(x)$ σε κάθε τμήμα του $[-2, 4]$.
  \item Να μελετήσετε τη μονοτονία της $f$ και να βρείτε τα ακρότατά της.
  \item Ολοκληρώνοντας τη $f'(x)$ σε κάθε τμήμα και χρησιμοποιώντας τη συνθήκη $f(1) = 3$, να βρείτε τον τύπο της $f(x)$ σε κάθε τμήμα. Στη συνέχεια, να υπολογίσετε το $\displaystyle\dint_{-2}^{4} f(x)\,dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 63ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 64ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο παρακάτω σχήμα δίνεται η γραφική παράσταση $C_f$ μιας δύο φορές παραγωγίσιμης συνάρτησης $f:[0, 6]\to\mathbb{R}$ με τα ακόλουθα γνωστά: $f(0) = 2$, $f(2) = 5$, $f(4) = 1$, $f(6) = 3$. Η $C_f$ παρουσιάζει τοπικό μέγιστο στο $x = 2$ και τοπικό ελάχιστο στο $x = 4$.

\begin{center}
\begin{tikzpicture}[scale=0.7]
  \draw[help lines, gray!30] (-1,-1) grid (7,6);
  \draw[-latex] (-1.3,0) -- (7.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,6.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3,4,5}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,2) (1,4) (2,5) (3,3) (4,1) (5,1.5) (6,3)};
  \filldraw[maincolor] (0,2) circle (2pt) node[left,font=\footnotesize] {$(0,2)$};
  \filldraw[maincolor] (2,5) circle (2pt) node[above,font=\footnotesize] {$(2,5)$};
  \filldraw[maincolor] (4,1) circle (2pt) node[below,font=\footnotesize] {$(4,1)$};
  \filldraw[maincolor] (6,3) circle (2pt) node[right,font=\footnotesize] {$(6,3)$};
  % horizontal reference lines
  \draw[dashed, gray!50] (0,2) -- (6,2);
  \draw[dashed, gray!50] (0,3) -- (6,3);
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αιτιολογήσετε ότι $f'(2) = 0$ και $f'(4) = 0$.
  \item Να εξηγήσετε γιατί δεν εφαρμόζεται το Θεώρημα \eng{Rolle} για την $f$ στο $[0, 6]$. Στη συνέχεια, με τη βοήθεια του Θεωρήματος \eng{Bolzano}, να αποδείξετε ότι η εξίσωση $f(x) = 2$ έχει τουλάχιστον δύο ρίζες $\rho_1, \rho_2$ στο $(0, 6)$.
  \item Να εφαρμόσετε το Θ.Μ.Τ. για την $f$ στα διαστήματα $[0, 2]$ και $[4, 6]$ και να αποδείξετε ότι υπάρχει $\xi_1 \in (0,2)$ με $f'(\xi_1) = \frac{3}{2}$ και $\xi_2 \in (4,6)$ με $f'(\xi_2) = 1$. Στη συνέχεια, να αποδείξετε ότι η εξίσωση $f'(x) = 1$ έχει τουλάχιστον μία λύση $x_1$ στο $(0, 2)$.
  \item Αξιοποιώντας τα συμπεράσματα του ερωτήματος Δ3, να αποδείξετε ότι υπάρχει τουλάχιστον ένα $x_0 \in (0, 6)$ τέτοιο ώστε $f''(x_0) = 0$. Ποια είναι η γεωμετρική ερμηνεία αυτού του σημείου για τη $C_f$;
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 64ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 65ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η γραφική παράσταση $C_{f'}$ της παραγώγου μιας δύο φορές παραγωγίσιμης συνάρτησης $f:[-2, 6]\to\mathbb{R}$. Η $f'$ μηδενίζεται στα $x = 0, 2, 5$ και αλλάζει πρόσημο σε κάθε ρίζα. Δίνονται επιπλέον οι τιμές: $f(-2) = 1$, $f(0) = 3$, $f(2) = -1$, $f(5) = 4$ και $f(6) = 2$.

\begin{center}
\begin{tikzpicture}[scale=0.6]
  \draw[help lines, gray!30] (-3,-4) grid (7,5);
  \draw[-latex] (-3.3,0) -- (7.3,0) node[right] {$x$};
  \draw[-latex] (0,-4.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {-2,-1,1,2,3,4,5,6}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-3,-2,-1,1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, secondcolor, smooth, tension=0.5]
    plot coordinates {(-2,3) (-1,2) (0,0) (1,-3) (2,0) (3,2) (4,3) (5,0) (5.5,-2) (6,-3)};
  \filldraw[secondcolor] (0,0) circle (2pt) node[above left,font=\footnotesize] {$0$};
  \filldraw[secondcolor] (2,0) circle (2pt) node[above right,font=\footnotesize] {$2$};
  \filldraw[secondcolor] (5,0) circle (2pt) node[above right,font=\footnotesize] {$5$};
  \node[secondcolor] at (6,2) {$C_{f'}$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να κατασκευάσετε τον πίνακα μεταβολών της $f$ και να βρείτε το ολικό μέγιστο καθώς και το ολικό ελάχιστο στο $[-2, 6]$.
  \item Να αποδείξετε ότι η εξίσωση $f(x) = 1$ έχει ακριβώς δύο ρίζες $\rho_1, \rho_2$ στο διάστημα $(0, 6)$.
  \item Εφαρμόζοντας το Θεώρημα \eng{Rolle} στα διαστήματα $[-2, \rho_1]$ και $[\rho_1, \rho_2]$, να δείξετε ότι η εξίσωση $f'(x) = 0$ έχει τουλάχιστον δύο ρίζες. Πώς επιβεβαιώνεται αυτό από το δοσμένο σχήμα;
  \item Με χρήση του Θ.Μ.Τ. στο διάστημα $[2, 5]$, να αποδείξετε ότι υπάρχει τουλάχιστον ένα $\xi \in (0, 5)$ τέτοιο ώστε $f'(\xi) = 1$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 65ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 66ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η γραφική παράσταση $C_f$ μιας παραγωγίσιμης και γνησίως αύξουσας συνάρτησης $f:[0, 5]\to\mathbb{R}$ με $f(0) = 0$ και $f(5) = 4$. Το σκιασμένο εμβαδόν του χωρίου που περικλείεται από τη $C_f$, τον άξονα $x'x$ και την ευθεία $x=5$, είναι ίσο με $E = 8$.

\begin{center}
\begin{tikzpicture}[scale=0.8]
  \draw[help lines, gray!30] (-1,-1) grid (6,5);
  \draw[-latex] (-1.3,0) -- (6.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % f
  \draw[very thick, maincolor, smooth, tension=0.4]
    plot coordinates {(0,0) (1,0.8) (2,1.5) (3,2.5) (4,3.3) (5,4)};
  % shaded area under f
  \fill[cyan!12, opacity=0.5, smooth, tension=0.4]
    (0,0) -- plot coordinates {(0,0) (1,0.8) (2,1.5) (3,2.5) (4,3.3) (5,4)} -- (5,0) -- cycle;
  \node at (2.5,1) {$E = 8$};
  % rectangle
  \draw[dashed, gray!50] (5,0) -- (5,4) -- (0,4);
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (5,4) circle (2pt) node[above right,font=\footnotesize] {$(5,4)$};
  \filldraw[maincolor] (3,2.5) circle (2pt) node[above left,font=\footnotesize] {$(3; 2{,}5)$};
  \node[maincolor] at (4,1.5) {$C_f$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αιτιολογήσετε γιατί η $f$ αντιστρέφεται και να βρείτε το πεδίο ορισμού της $f^{-1}$.
  \item Με βάση τη γεωμετρική ερμηνεία του εμβαδού στο ορθογώνιο $5 \times 4$, να δικαιολογήσετε τη σχέση $\dint_0^5 f(x)\,dx + \dint_0^4 f^{-1}(x)\,dx = 20$ και να υπολογίσετε το ολοκλήρωμα της αντίστροφης.
  \item Να αποδείξετε ότι υπάρχει $\xi \in (0, 5)$ τέτοιο ώστε $f'(\xi) = \frac{4}{5}$. Αν επιπλέον η $f$ είναι κυρτή στο $[0, 5]$, να αποδείξετε ότι η εφαπτομένη της $C_f$ στο σημείο $(5, 4)$ τέμνει τον άξονα $x'x$ σε σημείο με τετμημένη θετική.
  \item Γνωρίζοντας ότι για κυρτή συνάρτηση η $C_f$ βρίσκεται «κάτω» από τη χορδή που ενώνει τα άκρα της, να γράψετε την εξίσωση της χορδής και να επιβεβαιώσετε ότι $\dint_0^5 f(x)\,dx < 10$. Πώς συμφωνεί αυτό με τα δεδομένα της άσκησης;
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 66ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 67ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο παρακάτω σχήμα δίνεται η γραφική παράσταση της παραγώγου $f'$ μιας δύο φορές παραγωγίσιμης συνάρτησης $f:[0, 8]\to\mathbb{R}$, με $f(0) = 0$. Γνωρίζουμε ότι η $f'$ μηδενίζεται μόνο στα $x=0$, $x=4$ και $x=8$. Τα εμβαδά των χωρίων $E_+$ και $E_-$ είναι ίσα λόγω συμμετρίας. Επιπλέον, το σημείο $(2,3)$ είναι το μέγιστο της $f'$.

\begin{center}
\begin{tikzpicture}[scale=0.6]
  \draw[help lines, gray!30] (-1,-4) grid (9,4);
  \draw[-latex] (-1.3,0) -- (9.3,0) node[right] {$x$};
  \draw[-latex] (0,-4.3) -- (0,4.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6,7,8}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-3,-2,-1,1,2,3}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % f' symmetric: sin-like
  \draw[very thick, secondcolor, domain=0:8, samples=80] plot (\x, {3*sin((\x)*180/8)});
  \fill[secondcolor!12, opacity=0.4, domain=0:4, samples=40] (0,0) -- plot (\x, {3*sin((\x)*180/8)}) -- (4,0) -- cycle;
  \fill[red!10, opacity=0.4, domain=4:8, samples=40] (4,0) -- plot (\x, {3*sin((\x)*180/8)}) -- (8,0) -- cycle;
  \filldraw[secondcolor] (0,0) circle (2pt);
  \filldraw[secondcolor] (4,0) circle (2pt) node[below right,font=\footnotesize] {};
  \filldraw[secondcolor] (8,0) circle (2pt);
  \filldraw[secondcolor] (2,3) circle (2pt) node[above,font=\footnotesize] {$(2,3)$};
  \filldraw[secondcolor] (6,-3) circle (2pt) node[below,font=\footnotesize] {$(6,-3)$};
  \node[secondcolor] at (7.5,2) {$C_{f'}$};
  \node at (2,1) {$E_+$};
  \node at (6,-1) {$E_-$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να μελετήσετε την $f$ ως προς τη μονοτονία και τα ακρότατα. Στη συνέχεια, χρησιμοποιώντας την ισότητα των εμβαδών $E_+ = E_-$, να αποδείξετε ότι $f(8) = 0$.
  \item Να αιτιολογήσετε ότι το Θεώρημα \eng{Rolle} εφαρμόζεται για την $f$ στο $[0, 8]$ και να βρείτε το $\xi \in (0, 8)$ για το οποίο $f'(\xi) = 0$. Εφαρμόζοντας ξανά \eng{Rolle} για την $f'$, να αποδείξετε ότι η $f''$ έχει τουλάχιστον δύο ρίζες.
  \item Να αποδείξετε ότι για κάθε σταθερά $c \in (0, f(4))$, η εξίσωση $f(x) = c$ έχει ακριβώς δύο λύσεις στο διάστημα $(0, 8)$.
  \item Ένα υλικό σημείο $M(x, y)$ κινείται επί της $C_f$ έτσι ώστε η τετμημένη του να αυξάνεται με σταθερό ρυθμό $x'(t) = 1$ μονάδα/sec. Τη χρονική στιγμή $t_0$ που το υλικό σημείο διέρχεται από τη θέση με τετμημένη $x = 2$, να βρείτε τον ρυθμό μεταβολής της τεταγμένης του, καθώς και την εφαπτομένη της γωνίας $\omega$ που σχηματίζει η εφαπτομένη ευθεία της $C_f$ με τον άξονα $x'x$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 67ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 68ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Δίνονται οι γραφικές παραστάσεις $C_f$ (μπλε) και $C_g$ (πράσινη) δύο γνησίως αυξουσών συναρτήσεων $f, g:[0, 4]\to\mathbb{R}$ με $f(0) = g(0) = 0$, $f(4) = g(4) = 3$, αλλά $f(x) > g(x)$ για κάθε $x \in (0, 4)$.

\begin{center}
\begin{tikzpicture}[scale=0.8]
  \draw[help lines, gray!30] (-1,-1) grid (5,4);
  \draw[-latex] (-1.3,0) -- (5.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,4.3) node[above] {$y$};
  \foreach \x in {1,2,3,4}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % f (above)
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,0) (1,1.8) (2,2.5) (3,2.9) (4,3)};
  \node[maincolor] at (1,2.5) {$C_f$};
  % g (below)
  \draw[very thick, secondcolor!50!black, smooth, tension=0.5]
    plot coordinates {(0,0) (1,0.5) (2,1) (3,1.8) (4,3)};
  \node[secondcolor!50!black] at (3,1) {$C_g$};
  % shaded between
  \fill[yellow!15, opacity=0.5, smooth, tension=0.5]
    plot coordinates {(0,0) (1,1.8) (2,2.5) (3,2.9) (4,3)}
    -- plot[smooth, tension=0.5] coordinates {(4,3) (3,1.8) (2,1) (1,0.5) (0,0)} -- cycle;
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (4,3) circle (2pt) node[above right,font=\footnotesize] {$(4,3)$};
  \filldraw[secondcolor!50!black] (2,1) circle (2pt) node[below right,font=\footnotesize] {$(2,1)$};
  \filldraw[maincolor] (2,2.5) circle (2pt) node[above right,font=\footnotesize] {$(2; 2{,}5)$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αποδείξετε ότι υπάρχουν $\xi_1, \xi_2 \in (0, 4)$ τέτοια ώστε οι εφαπτόμενες των $C_f$ και $C_g$ στα σημεία $M(\xi_1, f(\xi_1))$ και $N(\xi_2, g(\xi_2))$ αντίστοιχα, να είναι παράλληλες στην ευθεία που διέρχεται από τα σημεία $O(0, 0)$ και $A(4, 3)$.
  \item Να αποδείξετε ότι υπάρχει $\xi \in (0, 4)$ τέτοιο ώστε η εφαπτομένη της $C_f$ στο σημείο με τετμημένη $\xi$ να είναι παράλληλη με την εφαπτομένη της $C_g$ στο σημείο με την ίδια τετμημένη.
  \item Να αποδείξετε ότι $\dint_0^4 f(x)\,dx > \dint_0^4 g(x)\,dx$.
  \item Να αποδείξετε ότι οι συναρτήσεις $f, g$ αντιστρέφονται και, στη συνέχεια, να δείξετε ότι $\dint_0^3 f^{-1}(x)\,dx < \dint_0^3 g^{-1}(x)\,dx$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 68ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 69ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_f$ μιας συνάρτησης $f:[0, 6]\to\mathbb{R}$ η οποία έχει συνεχή πρώτη παράγωγο, μαζί με δύο εφαπτόμενες: στο $A(1, 3)$ με κλίση $2$ και στο $B(4, 1)$ με κλίση $-1$. Γνωρίζουμε ότι η $C_f$ τέμνει τον άξονα $x'x$ μόνο στα $x = 0$ και $x = 5$.

\begin{center}
\begin{tikzpicture}[scale=0.7]
  \draw[help lines, gray!30] (-1,-2) grid (7,5);
  \draw[-latex] (-1.3,0) -- (7.3,0) node[right] {$x$};
  \draw[-latex] (0,-2.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-1,1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % curve
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,0) (0.5,1.2) (1,3) (2,4.2) (3,3.5) (4,1) (5,0) (6,-1)};
  % tangent at A(1,3) slope 2
  \draw[dashed, red!60!black] (0,1) -- (2,5);
  \node[red!60!black,font=\footnotesize] at (2,5.3) {$\varepsilon_A$};
  % tangent at B(4,1) slope -1
  \draw[dashed, red!60!black] (3,2) -- (5.5,-0.5);
  \node[red!60!black,font=\footnotesize] at (5.5,-0.8) {$\varepsilon_B$};
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (1,3) circle (2pt) node[above left,font=\footnotesize] {$A(1,3)$};
  \filldraw[maincolor] (4,1) circle (2pt) node[below right,font=\footnotesize] {$B(4,1)$};
  \filldraw[maincolor] (5,0) circle (2pt);
  \filldraw[maincolor] (6,-1) circle (2pt) node[right,font=\footnotesize] {$(6,-1)$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αποδείξετε ότι υπάρχει $\xi_1 \in (1, 4)$ τέτοιο, ώστε η εφαπτομένη της $C_f$ στο σημείο με τετμημένη $\xi_1$ να είναι παράλληλη στον άξονα $x'x$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 0$ έχει τουλάχιστον δύο ρίζες στο $(0, 5)$.
  \item Να αποδείξετε ότι υπάρχει $\xi_2 \in (0, 5)$ τέτοιο, ώστε $f'(\xi_2) = f(\xi_2)$.
  \item Να αποδείξετε ότι $\dint_0^5 f(x)\,dx > 0$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 69ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 70ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνονται η $C_f$ (μπλε) μιας γνησίως αύξουσας $f:[0, 4]\to[0, 3]$ και η $C_g$ (πράσινη) μιας γνησίως αύξουσας $g:[0, 3]\to[0, 4]$, ώστε $g = f^{-1}$. Γνωστά: $f(0)=0$, $f(1)=1$, $f(2)=2$, $f(4)=3$ και $g(0)=0$, $g(1)=1$, $g(2)=2$, $g(3)=4$.

\begin{center}
\begin{tikzpicture}[scale=0.8]
  \draw[help lines, gray!30] (-1,-1) grid (5,4);
  \draw[-latex] (-1.3,0) -- (5.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,4.3) node[above] {$y$};
  \foreach \x in {1,2,3,4}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[dashed, gray!50] (-1,-1) -- (4,4);
  % f: concave, below diagonal
  \draw[very thick, maincolor, smooth, tension=0.4]
    plot coordinates {(0,0) (0.5,0.6) (1,1) (1.5,1.4) (2,2) (3,2.6) (4,3)};
  \node[maincolor] at (3.5,1.5) {$C_f$};
  % g = f^{-1}: convex, above diagonal
  \draw[very thick, secondcolor!50!black, smooth, tension=0.4]
    plot coordinates {(0,0) (0.6,0.5) (1,1) (1.4,1.5) (2,2) (2.6,3) (3,4)};
  \node[secondcolor!50!black] at (1,2.5) {$C_g$};
  % shaded between
  \fill[yellow!12, opacity=0.5, smooth, tension=0.4]
    plot coordinates {(0,0) (0.5,0.6) (1,1) (1.5,1.4) (2,2) (2.6,3) (3,4)}
    -- (3,3) -- plot[smooth, tension=0.4] coordinates {(3,3) (4,3)} -- (4,3)
    -- (4,0) -- cycle;
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (1,1) circle (2pt);
  \filldraw[maincolor] (2,2) circle (2pt);
  \filldraw[maincolor] (4,3) circle (2pt) node[right,font=\footnotesize] {$(4,3)$};
  \filldraw[secondcolor!50!black] (3,4) circle (2pt) node[above,font=\footnotesize] {$(3,4)$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αποδείξετε ότι υπάρχουν $\xi_1 \in (0, 4)$ και $\xi_2 \in (0, 3)$ τέτοια, ώστε $f'(\xi_1) \cdot g'(\xi_2) = 1$.
  \item Να αποδείξετε ότι $\dint_0^4 f(x)\,dx + \dint_0^3 g(x)\,dx = 12$.
  \item Να αποδείξετε ότι υπάρχει $\xi \in (0, 2)$ τέτοιο, ώστε $f''(\xi) + g''(\xi) = 0$.
  \item Αν η $f$ είναι κοίλη, να αποδείξετε ότι $\dint_0^4 f(x)\,dx > 6$ και, στη συνέχεια, ότι $\dint_0^3 g(x)\,dx < 6$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 70ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 71ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_f$ μιας δύο φορές παραγωγίσιμης $f:[0, 6]\to\mathbb{R}$ με $f(0)=0$, τοπικό μέγιστο $f(2)=4$, τοπικό ελάχιστο $f(4)=-2$, $f(6)=1$. Η $C_f$ είναι κοίλη στο $(0,3)$ και κυρτή στο $(3,6)$.

\begin{center}
\begin{tikzpicture}[scale=0.65]
  \draw[help lines, gray!30] (-1,-3) grid (7,5);
  \draw[-latex] (-1.3,0) -- (7.3,0) node[right] {$x$};
  \draw[-latex] (0,-3.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-2,-1,1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,0) (1,3) (2,4) (3,1) (4,-2) (5,-1) (6,1)};
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (2,4) circle (2pt) node[above,font=\footnotesize] {$(2,4)$};
  \filldraw[maincolor] (4,-2) circle (2pt) node[below,font=\footnotesize] {$(4,-2)$};
  \filldraw[maincolor] (6,1) circle (2pt) node[right,font=\footnotesize] {$(6,1)$};
  \draw[dashed, gray!50] (0,1) -- (6,1);
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αποδείξετε ότι η εξίσωση $f(x) = 0$ έχει ακριβώς τρεις ρίζες στο διάστημα $[0, 6]$.
  \item Να λύσετε την ανίσωση $f(x) > 0$ στο διάστημα $(0, 6)$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = \frac{1}{6}$ έχει τουλάχιστον δύο ρίζες στο διάστημα $(0, 6)$.
  \item Να αποδείξετε ότι $f(x) \geq 2x$ για κάθε $x \in [0, 2]$, και στη συνέχεια ότι $\dint_0^2 f(x)\,dx > 4$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 71ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 72ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_{f'}$ μιας $f:[-2, 5]\to\mathbb{R}$ με $f(-2)=0$. Η $f'$ είναι μια λεία καμπύλη: $f'(-2)=3$, $f'(0)=0$, $f'(2)=-1$, $f'(4)=0$, $f'(5)=1$. Η $f'$ αλλάζει πρόσημο στα $x=0$ και $x=4$.

\begin{center}
\begin{tikzpicture}[scale=0.6]
  \draw[help lines, gray!30] (-3,-2) grid (6,4);
  \draw[-latex] (-3.3,0) -- (6.3,0) node[right] {$x$};
  \draw[-latex] (0,-2.3) -- (0,4.3) node[above] {$y$};
  \foreach \x in {-2,-1,1,2,3,4,5}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-1,1,2,3}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, secondcolor, smooth, tension=0.5]
    plot coordinates {(-2,3) (-1,2) (0,0) (1,-1) (2,-1) (3,-0.5) (4,0) (5,1)};
  \filldraw[secondcolor] (0,0) circle (2pt) node[above right,font=\footnotesize] {$0$};
  \filldraw[secondcolor] (4,0) circle (2pt) node[above right,font=\footnotesize] {$4$};
  \node[secondcolor] at (5,2.5) {$C_{f'}$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να μελετήσετε τη συνάρτηση $f$ ως προς τη μονοτονία και τα τοπικά ακρότατα. 
  \item Να αποδείξετε ότι $f(0) > 0$ και $f(4) < f(0)$.
  \item Να λύσετε την ανίσωση $f(x) \geq 0$ στο διάστημα $[-2, 5]$.
  \item Να αποδείξετε ότι η εξίσωση $f(x) = f(4)$ έχει ακριβώς δύο ρίζες στο διάστημα $[-2, 5]$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 72ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 73ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_{f''}$ (δεύτερη παράγωγος) μιας δύο φορές παραγωγίσιμης $f:[0, 6]\to\mathbb{R}$. Η $f''$ μηδενίζεται στα $x=2$ και $x=4$ (αλλαγή πρόσημου). Δίνεται: $f'(0) = 0$, $f'(6) = 0$, $f(0) = 1$.

\begin{center}
\begin{tikzpicture}[scale=0.65]
  \draw[help lines, gray!30] (-1,-3) grid (7,3);
  \draw[-latex] (-1.3,0) -- (7.3,0) node[right] {$x$};
  \draw[-latex] (0,-3.3) -- (0,3.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-2,-1,1,2}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, secondcolor, smooth, tension=0.5]
    plot coordinates {(0,2) (1,1) (2,0) (3,-2) (4,0) (5,1) (6,0.5)};
  \filldraw[secondcolor] (2,0) circle (2pt) node[above,font=\footnotesize] {$2$};
  \filldraw[secondcolor] (4,0) circle (2pt) node[above,font=\footnotesize] {$4$};
  \filldraw[secondcolor] (0,2) circle (2pt) node[above right,font=\footnotesize] {$(0,2)$};
  \node[secondcolor] at (6,2) {$C_{f''}$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να μελετήσετε τη συνάρτηση $f$ ως προς την κυρτότητα και να βρείτε τα σημεία καμπής της γραφικής της παράστασης.
  \item Να αποδείξετε ότι η συνάρτηση $f$ είναι γνησίως αύξουσα στο διάστημα $[0, 2]$.
  \item Να αποδείξετε ότι $f(x) > 1$ για κάθε $x \in (0, 2]$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 0$ έχει τουλάχιστον μία ρίζα στο διάστημα $(2, 6)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 73ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 74ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_f$ μιας παραγωγίσιμης $f:[0, 5]\to\mathbb{R}$ μαζί με τη χορδή $AB$ ($A(0,1)$, $B(5,6)$). Η $f$ είναι κοίλη (κυρτή προς τα πάνω) και $f(x) \geq \frac{x}{5}\cdot5 + 1 = x + 1$ (βρίσκεται πάνω από τη χορδή).

\begin{center}
\begin{tikzpicture}[scale=0.7]
  \draw[help lines, gray!30] (-1,-1) grid (6,8);
  \draw[-latex] (-1.3,0) -- (6.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,8.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3,4,5,6,7}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  % chord
  \draw[dashed, gray!50] (0,1) -- (5,6);
  % f (above chord, concave)
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,1) (1,3.5) (2,5) (3,6) (4,6.5) (5,6)};
  \filldraw[maincolor] (0,1) circle (2pt) node[left,font=\footnotesize] {$A(0,1)$};
  \filldraw[maincolor] (5,6) circle (2pt) node[right,font=\footnotesize] {$B(5,6)$};
  \filldraw[maincolor] (2,5) circle (2pt) node[above,font=\footnotesize] {$(2,5)$};
  \node[maincolor] at (4,4) {$C_f$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αποδείξετε ότι $f(x) \geq x + 1$ για κάθε $x \in [0, 5]$.
  \item Να λύσετε την εξίσωση $f(x) = x + 1$ στο διάστημα $[0, 5]$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 1$ έχει μοναδική ρίζα στο διάστημα $(0, 5)$.
  \item Να αποδείξετε ότι $\dint_0^5 f(x)\,dx > \frac{35}{2}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 74ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 75ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_f$ μιας $f:[0, 4]\to\mathbb{R}$ η οποία επαληθεύει $f(x) > 0$ για $x \in (0, 4)$, $f(0) = f(4) = 0$, $f(2) = 3$ (ολικό μέγιστο). Η $C_f$ είναι κυρτή στο $[0, 2]$ και κοίλη στο $[2, 4]$.

\begin{center}
\begin{tikzpicture}[scale=0.7]
  \draw[help lines, gray!30] (-1,-1) grid (5,4);
  \draw[-latex] (-1.3,0) -- (5.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,4.3) node[above] {$y$};
  \foreach \x in {1,2,3,4}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,0) (0.5,0.8) (1,1.8) (1.5,2.6) (2,3) (2.5,2.6) (3,1.8) (3.5,0.8) (4,0)};
  \fill[cyan!8, opacity=0.4, smooth, tension=0.5]
    (0,0) -- plot coordinates {(0,0) (0.5,0.8) (1,1.8) (1.5,2.6) (2,3) (2.5,2.6) (3,1.8) (3.5,0.8) (4,0)} -- cycle;
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (2,3) circle (2pt) node[above,font=\footnotesize] {$(2,3)$};
  \filldraw[maincolor] (4,0) circle (2pt);
  \draw[dashed, gray!50] (0,3) -- (4,3);
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 0$ έχει ακριβώς μία ρίζα στο διάστημα $(0, 4)$.
  \item Να αποδείξετε ότι η εξίσωση $f(x) = \frac{3}{2}$ έχει ακριβώς δύο ρίζες στο διάστημα $(0, 4)$.
  \item Να αποδείξετε ότι $\dint_0^4 f(x)\,dx < 12$.
  \item Να αποδείξετε ότι υπάρχει $\xi \in (0, 2)$ τέτοιο ώστε $f'(\xi) = \frac{3}{2}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 75ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 76ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_{f'}$ μιας δύο φορές παραγωγίσιμης $f:[0, 8]\to\mathbb{R}$. Η $f'$ είναι γνησίως φθίνουσα στο $[0, 4]$ (από $f'(0)=4$ στο $f'(4)=0$) και γνησίως αύξουσα στο $[4, 8]$ (από $f'(4)=0$ στο $f'(8)=2$). Δίνεται $f(0) = 0$.

\begin{center}
\begin{tikzpicture}[scale=0.6]
  \draw[help lines, gray!30] (-1,-1) grid (9,5);
  \draw[-latex] (-1.3,0) -- (9.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6,7,8}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, secondcolor, smooth, tension=0.4]
    plot coordinates {(0,4) (1,3) (2,2) (3,0.8) (4,0) (5,0.3) (6,0.8) (7,1.5) (8,2)};
  \filldraw[secondcolor] (0,4) circle (2pt) node[above right,font=\footnotesize] {$(0,4)$};
  \filldraw[secondcolor] (4,0) circle (2pt) node[below right,font=\footnotesize] {$(4,0)$};
  \filldraw[secondcolor] (8,2) circle (2pt) node[right,font=\footnotesize] {$(8,2)$};
  \node[secondcolor] at (7,3.5) {$C_{f'}$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να λύσετε την ανίσωση $f(x) \geq 0$ στο διάστημα $[0, 8]$.
  \item Να βρείτε τα διαστήματα κυρτότητας της $f$ και το σημείο καμπής της $C_f$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 2$ έχει ακριβώς δύο ρίζες στο διάστημα $[0, 8]$.
  \item Να αποδείξετε ότι $f(x) \leq f(4)$ για κάθε $x \in [0, 4]$ και $f(x) \geq f(4)$ για κάθε $x \in [4, 8]$. 
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 76ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 77ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_f$ μιας γνησίως αύξουσας $f:[0, 5]\to\mathbb{R}$ με $f(0)=-2$, $f(2) = 0$, $f(3) = 1$, $f(5) = 4$. Η $f$ είναι κοίλη στο $[0, 5]$.

\begin{center}
\begin{tikzpicture}[scale=0.7]
  \draw[help lines, gray!30] (-1,-3) grid (6,5);
  \draw[-latex] (-1.3,0) -- (6.3,0) node[right] {$x$};
  \draw[-latex] (0,-3.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-2,-1,1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, maincolor, smooth, tension=0.4]
    plot coordinates {(0,-2) (1,-0.5) (2,0) (3,1) (4,2.5) (5,4)};
  % chord from (0,-2) to (5,4)
  \draw[dashed, gray!50] (0,-2) -- (5,4);
  \filldraw[maincolor] (0,-2) circle (2pt) node[left,font=\footnotesize] {$(0,-2)$};
  \filldraw[maincolor] (2,0) circle (2pt) node[above left,font=\footnotesize] {$(2,0)$};
  \filldraw[maincolor] (3,1) circle (2pt);
  \filldraw[maincolor] (5,4) circle (2pt) node[right,font=\footnotesize] {$(5,4)$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να λύσετε την ανίσωση $f(x) > 0$ στο διάστημα $[0, 5]$.
  \item Να αποδείξετε ότι $f(x) \geq \frac{6}{5}x - 2$ για κάθε $x \in [0, 5]$.
  \item Να αποδείξετε ότι υπάρχει $\xi_1 \in (0, 5)$ τέτοιο ώστε $f'(\xi_1) = \frac{6}{5}$.
  \item Να αποδείξετε ότι $f'(2) > \frac{4}{3} > f'(5)$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 77ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 78ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στα σχήματα δίνονται \textbf{χωριστά} η $C_f$ (μπλε) και η $C_{f'}$ (κόκκινη) μιας $f:[-1, 5]\to\mathbb{R}$. Από τη γραφική: $f(-1) = 2$, $f(1) = 4$ (μέγιστο), $f(3) = 0$ (ρίζα), $f(5) = -2$.

\begin{center}
\begin{tikzpicture}[scale=0.6]
  \draw[help lines, gray!30] (-2,-3) grid (6,5);
  \draw[-latex] (-2.3,0) -- (6.3,0) node[right] {$x$};
  \draw[-latex] (0,-3.3) -- (0,5.3) node[above] {$y$};
  \foreach \x in {-1,1,2,3,4,5}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-2,-1,1,2,3,4}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(-1,2) (0,3.5) (1,4) (2,2.5) (3,0) (4,-1.5) (5,-2)};
  \node[maincolor] at (-1,3.5) {$C_f$};
  \draw[very thick, secondcolor, smooth, tension=0.5]
    plot coordinates {(-1,2) (0,1) (1,0) (2,-1.5) (3,-1.5) (4,-1) (5,-0.3)};
  \node[secondcolor] at (4.5,-2.5) {$C_{f'}$};
  \filldraw[maincolor] (1,4) circle (2pt) node[above,font=\footnotesize] {$(1,4)$};
  \filldraw[maincolor] (3,0) circle (2pt);
  \filldraw[secondcolor] (1,0) circle (2pt);
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να λύσετε την ανίσωση $f(x) > 0$ στο διάστημα $[-1, 5]$.
  \item Να αποδείξετε ότι $f(x) \leq 4$ για κάθε $x \in [-1, 5]$.
  \item Να αποδείξετε ότι η εξίσωση $f(x) + f'(x) = 0$ έχει τουλάχιστον μία ρίζα στο διάστημα $(1, 3)$.
  \item Να αποδείξετε ότι $\dint_{-1}^{5} f(x)\,dx < 24$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 78ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 79ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_{f''}$ μιας δύο φορές παραγωγίσιμης $f:[0, 4]\to\mathbb{R}$. Η $f''(x) = 2 - x$ (ευθεία γραμμή, $f''(0)=2$, $f''(2)=0$, $f''(4) = -2$). Δίνεται $f'(0) = -1$ και $f(0) = 3$.

\begin{center}
\begin{tikzpicture}[scale=0.8]
  \draw[help lines, gray!30] (-1,-3) grid (5,3);
  \draw[-latex] (-1.3,0) -- (5.3,0) node[right] {$x$};
  \draw[-latex] (0,-3.3) -- (0,3.3) node[above] {$y$};
  \foreach \x in {1,2,3,4}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {-2,-1,1,2}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[very thick, secondcolor] (0,2) -- (4,-2);
  \filldraw[secondcolor] (0,2) circle (2pt) node[above right,font=\footnotesize] {$(0,2)$};
  \filldraw[secondcolor] (2,0) circle (2pt) node[above right,font=\footnotesize] {$(2,0)$};
  \filldraw[secondcolor] (4,-2) circle (2pt) node[below right,font=\footnotesize] {$(4,-2)$};
  \node[secondcolor] at (3.5,1.5) {$C_{f''}$};
  \fill[secondcolor!12, opacity=0.5] (0,0) -- (0,2) -- (2,0) -- cycle;
  \fill[red!10, opacity=0.5] (2,0) -- (4,-2) -- (4,0) -- cycle;
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να μελετήσετε τη συνάρτηση $f$ ως προς την κυρτότητα και να βρείτε τα σημεία καμπής της $C_f$.
  \item Να υπολογίσετε την τιμή $f'(2)$, γνωρίζοντας ότι το εμβαδόν του τριγώνου που σχηματίζει η $C_{f''}$ με τους άξονες στο 1ο τεταρτημόριο ισούται με $2$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 0$ έχει ακριβώς μία ρίζα στο διάστημα $(0, 2)$.
  \item Να λύσετε την ανίσωση $f'(x) > 0$ στο διάστημα $[0, 4]$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 79ο ΘΕΜΑ Δ ===========

%---------------------------------------------------------------

%=========== 80ο ΘΕΜΑ Δ ===========
\begin{thema}{Δ}
Στο σχήμα δίνεται η $C_f$ μιας $f:[0, 6]\to\mathbb{R}$ (μπλε) μαζί με την ευθεία $y = x$ (διακεκομμένη). Η $C_f$ τέμνει τη $y = x$ ακριβώς στα $x = 0$, $x = 3$ και $x = 5$. Η $f$ είναι δύο φορές παραγωγίσιμη, $f(2) = 3$, $f(4) = 3$.

\begin{center}
\begin{tikzpicture}[scale=0.7]
  \draw[help lines, gray!30] (-1,-1) grid (7,7);
  \draw[-latex] (-1.3,0) -- (7.3,0) node[right] {$x$};
  \draw[-latex] (0,-1.3) -- (0,7.3) node[above] {$y$};
  \foreach \x in {1,2,3,4,5,6}
    \draw (\x,0.1) -- (\x,-0.1) node[below,font=\footnotesize] {$\x$};
  \foreach \y in {1,2,3,4,5,6}
    \draw (0.1,\y) -- (-0.1,\y) node[left,font=\footnotesize] {$\y$};
  \draw[dashed, gray!50] (0,0) -- (6,6);
  \draw[very thick, maincolor, smooth, tension=0.5]
    plot coordinates {(0,0) (1,2) (2,3) (3,3) (4,3) (5,5) (6,6.5)};
  \filldraw[maincolor] (0,0) circle (2pt);
  \filldraw[maincolor] (2,3) circle (2pt) node[above left,font=\footnotesize] {$(2,3)$};
  \filldraw[maincolor] (3,3) circle (2pt) node[below right,font=\footnotesize] {$(3,3)$};
  \filldraw[maincolor] (5,5) circle (2pt) node[below right,font=\footnotesize] {$(5,5)$};
\end{tikzpicture}
\end{center}

\begin{erwthma}
  \item Να λύσετε την εξίσωση $f(x) = x$ και την ανίσωση $f(x) > x$ στο διάστημα $[0, 6]$.
  \item Να αποδείξετε ότι η εξίσωση $f'(x) = 1$ έχει τουλάχιστον δύο ρίζες στο διάστημα $(0, 5)$.
  \item Να αποδείξετε ότι υπάρχει $\eta \in (0, 5)$ τέτοιο ώστε $f''(\eta) = 0$.
  \item Να αποδείξετε ότι $\dint_0^3 f(x)\,dx > \frac{9}{2}$.
\end{erwthma}
\end{thema}
%=========== ΤΕΛΟΣ - 80ο ΘΕΜΑ Δ ===========