*   Function: $f(x) = x^4 - 2x^2 + \lambda$, $\lambda > 0$.
    *   Condition: The x-axis ($y=0$) is tangent to the graph exactly at the local minima.

    *   **Part 1: Prove $\lambda = 1$.**
        *   Find $f'(x)$.
        *   $f'(x) = 4x^3 - 4x = 4x(x^2 - 1) = 4x(x-1)(x+1)$.
        *   Critical points: $x=0, x=1, x=-1$.
        *   Sign of $f'(x)$:
            *   $x \in (-\infty, -1) \implies f'(x) < 0$
            *   $x \in (-1, 0) \implies f'(x) > 0$
            *   $x \in (0, 1) \implies f'(x) < 0$
            *   $x \in (1, +\infty) \implies f'(x) > 0$
        *   Local minima occur at $x=1$ and $x=-1$.
        *   Since the x-axis is tangent at local minima, $f(1) = 0$ and $f(-1) = 0$.
        *   $f(1) = 1^4 - 2(1)^2 + \lambda = 0 \implies 1 - 2 + \lambda = 0 \implies \lambda - 1 = 0 \implies \lambda = 1$.
        *   $f(-1) = (-1)^4 - 2(-1)^2 + 1 = 1 - 2 + 1 = 0$. Correct.

    *   **Part 2: Prove $\int_{0}^{a} f(x) \, dx \ge 0$ for all $a \in \mathbb{R}$.**
        *   $f(x) = x^4 - 2x^2 + 1 = (x^2 - 1)^2$.
        *   Since $f(x)$ is a square of a real expression, $f(x) \ge 0$ for all $x \in \mathbb{R}$.
        *   If $a \ge 0$, the integral of a non-negative function is non-negative.
        *   If $a < 0$, $\int_{0}^{a} f(x) \, dx = -\int_{a}^{0} f(x) \, dx$. Wait, this would be negative? Let's re-read.
        *   Wait, if $a < 0$, $\int_{0}^{a} f(x) \, dx = - \int_{a}^{0} f(x) \, dx$. Since $f(x) \ge 0$, $\int_{a}^{0} f(x) \, dx \ge 0$, so the result is $\le 0$.
        *   Let me re-read the prompt: "$\int_{0}^{a} f(x) \ge 0$ για κάθε $a \in \mathbb{R}$".
        *   Wait, is $f(x)$ always non-negative? Yes, $f(x) = (x^2-1)^2$.
        *   Is the integral always non-negative?
            *   For $a=0$, it's 0.
            *   For $a > 0$, $\int_0^a f(x) dx \ge 0$ because $f(x) \ge 0$.
            *   For $a < 0$, $\int_0^a f(x) dx = - \int_a^0 f(x) dx \le 0$.
        *   Let me check the problem statement again. $f(x) = x^4 - 2x^2 + \lambda$.
        *   Maybe the integral is $\int_{-a}^{a} f(x) dx$? No, it says $\int_{0}^{a}$.
        *   Wait, maybe I should evaluate the integral?
            *   $F(x) = \int (x^4 - 2x^2 + 1) dx = \frac{x^5}{5} - \frac{2x^3}{3} + x$.
            *   $G(a) = \int_0^a f(x) dx = \frac{a^5}{5} - \frac{2a^3}{3} + a = a(\frac{a^4}{5} - \frac{2a^2}{3} + 1)$.
            *   Let $u = a^2$. The quadratic is $Q(u) = \frac{1}{5}u^2 - \frac{2}{3}u + 1$.
            *   Discriminant $\Delta = (-\frac{2}{3})^2 - 4(\frac{1}{5})(1) = \frac{4}{9} - \frac{4}{5} = \frac{20-36}{45} = -\frac{16}{45} < 0$.
            *   Since $\Delta < 0$ and the coefficient of $u^2$ is positive, $Q(u) > 0$ for all $u$.
            *   Thus, $G(a) = a \cdot (\text{positive number})$.
            *   If $a < 0$, $G(a) < 0$.
        *   Is there a typo in the prompt? "$\int_{0}^{a} f(x) \ge 0$ for every $a \in \mathbb{R}$".
        *   Wait, if $f(x) = (x^2-1)^2$, then $f(x)$ is even.
        *   If $a < 0$, let $a = -b$ where $b > 0$.
        *   $\int_0^{-b} f(x) dx = - \int_{-b}^0 f(x) dx$. Since $f(x)$ is even, $\int_{-b}^0 f(x) dx = \int_0^b f(x) dx$.
        *   So $\int_0^{-b} f(x) dx = - \int_0^b f(x) dx$.
        *   If the prompt is strictly as written, and $f(x) = (x^2-1)^2$, then for $a < 0$ the integral is negative.
        *   Let me re-read the function: $f(x) = x^4 - 2x^2 + \lambda$. $\lambda = 1$.
        *   Is it possible $\lambda$ is something else? "The x-axis is tangent... exactly at the local minima". Minima are at $x = \pm 1$. $f(1) = 1-2+\lambda = 0 \implies \lambda=1$.
        *   Let me re-read the integral sign. $\dint_0^a f(x) \ge 0$.
        *   Maybe it's $a \ge 0$? "για κάθε $a \in \mathbb{R}$".
        *   Maybe it's $\int_{-a}^a$? No.
        *   Maybe $f(x)$ is different? No.
        *   Could $f(x)$ be $x^4 - 2x^2 + 1$? Yes.
        *   Let's check the expression $G(a) = \frac{a^5}{5} - \frac{2a^3}{3} + a$.
        *   $G'(a) = f(a) = (a^2-1)^2 \ge 0$.
        *   So $G(a)$ is a $\textbf{γνησίως αύξουσα}$ function.
        *   Since $G(0) = 0$, then for $a > 0$, $G(a) > 0$, and for $a < 0$, $G(a) < 0$.
        *   This is a contradiction to the prompt's "$\ge 0$ για κάθε $a \in \mathbb{R}$".
        *   Let me check if I misread the function. $f(x) = x^4 - 2x^2 + \lambda$.
        *   Could it be $\int_0^a |f(x)| dx$? No.
        *   Could the lower limit be something else? Or maybe the condition was $\lambda=1$ and the integral part meant something else?
        *   Let's search for this specific problem online or in textbooks. It looks like a standard problem. Often, such problems might have $a \in [0, +\infty)$.
        *   If the prompt says "για κάθε $a \in \mathbb{R}$", and $G(a)$ is strictly increasing with $G(0)=0$, it's impossible unless $f(x) = 0$.
        *   What if $\lambda$ was different? But $\lambda$ is determined by the tangency condition.
        *   Wait, maybe the function is $f(x) = (x^2-1)^2$ and the integral is $\int_0^a f(x) dx \ge 0$ for $a \ge 0$? That's trivial.
        *   Let's look at the prompt again. "$\dint_{0}^{a}f(x) \ge 0$ για κάθε $a\in\mathbb{R}$".
        *   Could $f(x)$ be defined as something else? No.
        *   Let's re-evaluate the integral of $f(x) = x^4 - 2x^2 + 1$.
        *   $G(a) = \frac{a^5}{5} - \frac{2a^3}{3} + a = a(\frac{3a^4 - 10a^2 + 15}{15})$.
        *   The quadratic $3u^2 - 10u + 15$ has $\Delta = 100 - 4(3)(15) = 100 - 180 = -80 < 0$.
        *   So $3a^4 - 10a^2 + 15$ is always positive.
        *   Thus $G(a)$ has the same sign as $a$.
        *   Is it possible the lower limit is not $0$?
        *   If the prompt says $\int_0^a f(x) dx \ge 0$ for all $a \in \mathbb{R}$, it's mathematically incorrect for this $f(x)$.
        *   HOWEVER, usually in these exams, there might be a typo in the problem statement provided by the user, or I'm missing something subtle.
        *   Let's check if $f(x)$ could be something like $|x^4 - 2x^2 + 1|$. No, that's the same.
        *   What if the integral was $\int_0^a x f(x) dx$?
        *   Let's assume there is a typo and it should be $a \ge 0$ or the expression is $\int_{-a}^a$. But if I must solve it as written... I will point out $f(x) \ge 0$ and then evaluate $G(a)$.
        *   Wait! Let's look at the expression again. $G(a) = \int_0^a f(x) dx$. If $a < 0$, we are integrating backwards.
        *   Actually, if the prompt says "Prove that...", and it's a known exercise, maybe I should check the function again.
        *   What if $f(x) = |x^4 - 2x^2 + 1|$? Same.
        *   What if the question was $\int_{a}^{0} f(x) dx$? No.
        *   Let's assume the intention was $a \ge 0$ or the user made a typo and actually the function is different. But I must use the provided $f(x)$.
        *   Wait, if $f(x) \ge 0$, then for $a > 0$, $\int_0^a f(x) dx \ge 0$. For $a < 0$, $\int_0^a f(x) dx \le 0$.
        *   Is it possible that $f(x)$ was meant to be something that is odd? No, $f$ is even.
        *   Let's consider the possibility that the question meant $\int_0^a f(x) dx$ where $a$ is a specific constant? No, "για κάθε $a \in \mathbb{R}$".
        *   Okay, I will perform the calculation and show $G(a) = a \cdot (\dots)$. If $a$ can be any real number, the statement is false.
        *   Let me search for this problem. $\dots f(x) = x^4 - 2x^2 + 1$. $\int_0^a f(x) dx$.
        *   Many similar problems in Greek textbooks use $a > 0$. I will assume the most logical interpretation (maybe $a \in [0, +\infty)$) or just calculate it and state the condition.
        *   Actually, looking at the source of such problems (Greek Panhellenic exams), they often have typos in the user-provided prompts. I will proceed by calculating the integral and showing it's non-negative for $a \ge 0$ and then just write the proof as if it's correct, but carefully.
        *   Actually, if I just say $f(x) \ge 0 \implies \int_0^a f(x) dx \ge 0$ for $a \ge 0$, and for $a < 0$ it's $\int_0^a f(x) dx = -\int_a^0 f(x) dx \le 0$.
        *   Wait! Is there any other $\lambda$? $f'(x) = 4x^3 - 4x$. Minima at $x = \pm 1$. $f(\pm 1) = 1 - 2 + \lambda = 0 \implies \lambda = 1$.
        *   Is there any other way the x-axis is tangent? Tangency means $f(x) = 0$ and $f'(x) = 0$. This happens at $x = \pm 1$ if $\lambda = 1$.
        *   Could the function be $f(x) = (x^2-1)^2$? Yes.
        *   I will assume the prompt meant $a \ge 0$ or $\int_{-a}^a$. But I'll write the solution for $\int_0^a f(x) dx$ for $a \ge 0$ and maybe mention the $a < 0$ case or just ignore it if it's a typo.
        *   Actually, let's try calculating $\int_0^a f(x) dx$ and see if I missed a sign.
        *   $G(a) = \frac{a^5}{5} - \frac{2a^3}{3} + a$.
        *   $G(1) = 1/5 - 2/3 + 1 = (3 - 10 + 15)/15 = 8/15 > 0$.
        *   $G(-1) = -1/5 + 2/3 - 1 = (-3 + 10 - 15)/15 = -8/15 < 0$.
        *   Definitely depends on the sign of $a$.
        *   I will solve it for $a \in \mathbb{R}$ and show the result, but maybe the prompt is slightly wrong and meant $a \ge 0$. In an exam, a student would likely assume $a \ge 0$ or just write the derivation. I'll write the derivation.

    *   **Part 3: Number of roots of $f(x) = k$ for $k \in (0, 1)$.**
        *   $f(x) = x^4 - 2x^2 + 1$.
        *   $f'(x) = 4x(x-1)(x+1)$.
        *   Variation table:
            *   $x \to -\infty, f(x) \to +\infty$.
            *   $x = -1, f(-1) = 0$ (local min).
            *   $x = 0, f(0) = 1$ (local max).
            *   $x = 1, f(1) = 0$ (local min).
            *   $x \to +\infty, f(x) \to +\infty$.
        *   For $k \in (0, 1)$:
            *   One root in $(-\infty, -1)$ because $f$ goes from $+\infty$ to $0$.
            *   One root in $(-1, 0)$ because $f$ goes from $0$ to $1$.
            *   One root in $(0, 1)$ because $f$ goes from $1$ to $0$.
            *   One root in $(1, +\infty)$ because $f$ goes from $0$ to $+\infty$.
        *   Total: 4 roots.

    *   **Part 4: Existence of the limit $\lim_{x \to 1} \frac{\sqrt{f(x)}}{x-1}$.**
        *   $f(x) = (x^2-1)^2$.
        *   $\sqrt{f(x)} = \sqrt{(x^2-1)^2} = |x^2-1|$.
        *   Limit: $\lim_{x \to 1} \frac{|x^2-1|}{x-1}$.
        *   Left limit ($x \to 1^-$):
            *   $x < 1 \implies x^2 < 1 \implies x^2-1 < 0$.
            *   $|x^2-1| = -(x^2-1)$.
            *   $\lim_{x \to 1^-} \frac{-(x^2-1)}{x-1} = \lim_{x \to 1^-} \frac{-(x-1)(x+1)}{x-1} = \lim_{x \to 1^-} -(x+1) = -2$.
        *   Right limit ($x \to 1^+$):
            *   $x > 1 \implies x^2 > 1 \implies x^2-1 > 0$.
            *   $|x^2-1| = x^2-1$.
            *   $\lim_{x \to 1^+} \frac{x^2-1}{x-1} = \lim_{x \to 1^+} \frac{(x-1)(x+1)}{x-1} = \lim_{x \to 1^+} (x+1) = 2$.
        *   Since the left and right limits are different ($-2 \neq 2$), the limit does not exist.

    *   The prompt for Part 2 says $\int_0^a f(x) dx \ge 0$ for $a \in \mathbb{R}$.
    *   Given $f(x) = (x^2-1)^2 \ge 0$, the integral $\int_0^a f(x) dx$ is only $\ge 0$ if $a \ge 0$.
    *   If $a < 0$, $\int_0^a f(x) dx = -\int_a^0 f(x) dx \le 0$.
    *   Could there be a different $\lambda$?
    *   "The x-axis is tangent... exactly at the local minima".
    *   This implies $f(x_{min}) = 0$.
    *   $f'(x) = 4x^3 - 4x$. Minima at $x = \pm 1$.
    *   $f(1) = 1 - 2 + \lambda = 0 \implies \lambda = 1$.
    *   Is it possible that $f(x)$ was $f(x) = x^4 - 2x^2 + 1$ but the integral was $\int_{-a}^a$? No, prompt says $\int_0^a$.
    *   Wait! Is it possible the function is $f(x) = |x^4 - 2x^2 + \lambda|$? No.
    *   What if the integral is $\int_0^a |f(x)| dx$? No, the absolute value is inside.
    *   What if the integral is $\int_0^a f(x) dx$ and $a$ was intended to be $a \ge 0$? This is common.
    *   What if the function was $f(x) = (x^2-1)^2$ and the prompt was $\int_0^a f(x) dx \ge 0$ for $a \in \mathbb{R}$? This is simply false for $a < 0$.
    *   However, I will write the solution demonstrating the calculation and then concluding based on the result. Or, more likely, I will just assume $a \ge 0$ and mention it, or just calculate the definite integral $G(a)$ and check if I missed something.
    *   Let me re-calculate $G(a) = \frac{1}{5}a^5 - \frac{2}{3}a^3 + a$.
    *   $G(a) = \frac{a}{15}(3a^4 - 10a^2 + 15)$.
    *   $3a^4 - 10a^2 + 15$ is always positive because its discriminant as a quadratic in $a^2$ is negative.
    *   So $\text{sgn}(G(a)) = \text{sgn}(a)$.
    *   There's no way $\int_0^a f(x) dx \ge 0$ for all $a \in \mathbb{R}$.
    *   I'll assume there's a typo in the prompt's "$a \in \mathbb{R}$" and it should be "$a \ge 0$". But I will solve it for $a \ge 0$ as requested by the "Prove" structure, perhaps adding a note or just proceeding. Usually, in these LaTeX tasks, if the prompt has a typo, I should just provide the "correct" intended mathematical proof.
    *   Wait, "$\int_0^a f(x) \ge 0$" for $a \in \mathbb{R}$. What if $f(x)$ is an odd function? No, it's even.
    *   What if the integral was $\int_0^a f(t) dt \cdot a \ge 0$? No.
    *   Okay, I will assume the user meant $a \ge 0$. In the context of Greek high school math, this is a very common typo. I'll write it for $a \ge 0$.

    *   $\lambda = 1$:
        *   $f'(x) = 4x^3 - 4x = 4x(x-1)(x+1)$.
        *   Roots: $-1, 0, 1$.
        *   $f'(x)$ signs: $(-)$ on $(-\infty, -1)$, $(+)$ on $(-1, 0)$, $(-)$ on $(0, 1)$, $(+)$ on $(1, +\infty)$.
        *   Minima at $x = \pm 1$.
        *   Tangency $\implies f(1) = 0 \implies 1 - 2 + \lambda = 0 \implies \lambda = 1$.
    *   Integral $\int_0^a f(x) dx \ge 0$:
        *   $f(x) = (x^2-1)^2 \ge 0$.
        *   For $a \ge 0$, $\int_0^a f(x) dx \ge 0$ since the integrand is non-negative.
        *   (I'll treat the $a \in \mathbb{R}$ as a typo for $a \in [0, +\infty)$ or just write the general form $G(a) = \frac{a}{15}(\dots)$ and note that for $a \ge 0$ it's $\ge 0$).
    *   $f(x) = k, k \in (0, 1)$:
        *   $f(x)$ variation: $+\infty \to 0 \to 1 \to 0 \to +\infty$.
        *   $k \in (0, 1)$ is between the local min (0) and local max (1).
        *   4 roots.
    *   Limit $\lim_{x \to 1} \frac{\sqrt{f(x)}}{x-1}$:
        *   $\sqrt{f(x)} = |x^2-1|$.
        *   Right limit: $x+1 \to 2$.
        *   Left limit: $-(x+1) \to -2$.
        *   Limit doesn't exist.

    *   `\begin{thema}{Γ}`
    *   `\begin{erwthma}`
    *   `\item`
    *   `\eng{Lagrange}` etc. (not needed here, but `\textbf{...}`)
    *   `\begin{align*}` for calculations.
    *   `\d x` for differentials.
    *   No indefinite integrals.
    *   Definite integrals: $\int_0^a f(x) \d x = [\dots]_0^a$.

    *   $f(x) = x^4 - 2x^2 + 1$.
    *   $f'(x) = 4x^3 - 4x$.
    *   Critical points $x = 0, \pm 1$.
    *   $f''(x) = 12x^2 - 4$.
    *   $f''(\pm 1) = 12 - 4 = 8 > 0$ (local min).
    *   $f''(0) = -4 < 0$ (local max).
    *   (The guide says "No 2nd derivative test for extrema", so I'll use the sign of $f'(x)$).
%=========== ΛΥΣΗ - ΘΕΜΑ Γ ===========

\begin{thema}{Γ}
\begin{erwthma}
\item 
Η παράγωγος της συνάρτησης $f$ είναι:
\[ f'(x) = 4x^3 - 4x = 4x(x^2 - 1) = 4x(x-1)(x+1) \]
Οι κρίσιμες τιμές της $f$ είναι οι ρίζες της εξίσωσης $f'(x) = 0$, δηλαδή οι $x = 0$, $x = 1$ και $x = -1$.
Με βάση το πρόσημο της $f'(x)$, έχουμε:
\begin{itemize}
    \item $f'(x) < 0$ για $x \in (-\infty, -1) \cup (0, 1)$
    \item $f'(x) > 0$ για $x \in (-1, 0) \cup (1, +\infty)$
\end{itemize}
Συνεπώς, η συνάρτηση $f$ παρουσιάζει \textbf{τοπικά ελάχιστα} στα $x = -1$ και $x = 1$.
Αφού ο άξονας $x'x$ εφάπτεται στη γραφική παράσταση ακριβώς στα τοπικά ελάχιστα, ισχύει $f(1) = 0$ και $f(-1) = 0$.
Από τη σχέση $f(1) = 0$ έχουμε:
\[ 1^4 - 2(1)^2 + \lambda = 0 \Leftrightarrow 1 - 2 + \lambda = 0 \Leftrightarrow \lambda = 1 \]
Ελέγχουμε για $x = -1$:
\[ f(-1) = (-1)^4 - 2(-1)^2 + 1 = 1 - 2 + 1 = 0 \]
Άρα, $\lambda = 1$.

\item 
Για $\lambda = 1$, η συνάρτηση είναι $f(x) = x^4 - 2x^2 + 1 = (x^2 - 1)^2$.
Παρατηρούμε ότι για κάθε $x \in \mathbb{R}$, ισχύει $f(x) \ge 0$ καθώς είναι τετράγωνο πραγματικού αριθμού.
Για $a \ge 0$, το ολοκλήρωμα μιας μη αρνητικής συνάρτησης είναι μη αρνητικό:
\[ \int_{0}^{a} f(x) \d x \ge 0 \]
Για $a < 0$, θέτουμε $a = -b$ με $b > 0$. Τότε:
\[ \int_{0}^{-b} f(x) \d x = -\int_{-b}^{0} f(x) \d x \]
Αφού $f(x)$ είναι άρτια συνάρτηση, $\int_{-b}^{0} f(x) \d x = \int_{0}^{b} f(x) \d x \ge 0$, άρα $\int_{0}^{a} f(x) \d x \le 0$.
\textit{Σημείωση: Η εκφώνηση $\int_{0}^{a} f(x) \d x \ge 0$ για κάθε $a \in \mathbb{R}$ είναι ισχύουσα για $a \ge 0$. Υπολογίζοντας το ορισμένο ολοκλήρωμα:}
\[ \int_{0}^{a} (x^4 - 2x^2 + 1) \d x = \left[ \frac{x^5}{5} - \frac{2x^3}{3} + x \right]_{0}^{a} = \frac{a^5}{5} - \frac{2a^3}{3} + a = \frac{a(3a^4 - 10a^2 + 15)}{15} \]
Το τριώνυμο $3u^2 - 10u + 15$ (με $u=a^2$) έχει διακρίνουσα $\Delta = (-10)^2 - 4(3)(15) = 100 - 180 = -80 < 0$, άρα είναι πάντα θετικό.
Συνεπώς, το πρόσημο του ολοκληρώματος ταυτίζεται με το πρόσημο του $a$.

\item 
Η εξίσωση είναι $f(x) = k$, όπου $k \in (0, 1)$. 
Από την ανάλυση της μονοτονίας της $f$ στο ερώτημα (α), έχουμε:
\begin{itemize}
    \item Στο διάστημα $(-\infty, -1)$, η $f$ είναι \textbf{γνησίως φθίνουσα} και παίρνει τιμές από $+\infty$ έως $f(-1)=0$. Άρα υπάρχει μία ρίζα στο $(-\infty, -1)$.
    \item Στο διάστημα $(-1, 0)$, η $f$ είναι \textbf{γνησίως αύξουσα} και παίρνει τιμές από $f(-1)=0$ έως $f(0)=1$. Άρα υπάρχει μία ρίζα στο $(-1, 0)$.
    \item Στο διάστημα $(0, 1)$, η $f$ είναι \textbf{γνησίως φθίνουσα} και παίρνει τιμές από $f(0)=1$ έως $f(1)=0$. Άρα υπάρχει μία ρίζα στο $(0, 1)$.
    \item Στο διάστημα $(1, +\infty)$, η $f$ είναι \textbf{γνησίως αύξουσα} και παίρνει τιμές από $f(1)=0$ έως $+\infty$. Άρα υπάρχει μία ρίζα στο $(1, +\infty)$.
\end{itemize}
Συνολικά, η εξίσωση $f(x) = k$ έχει $4$ διακριτές ρίζες για $k \in (0, 1)$.

\item 
Έχουμε $\sqrt{f(x)} = \sqrt{(x^2 - 1)^2} = |x^2 - 1|$. 
Εξετάζουμε τα πλευρικά όρια της συνάρτησης $g(x) = \dfrac{|x^2 - 1|}{x - 1}$ στο $x = 1$:
\begin{itemize}
    \item Για $x \to 1^+$, ισχύει $x > 1 \Rightarrow x^2 - 1 > 0$, οπότε $|x^2 - 1| = x^2 - 1$:
    \[ \lim_{x \to 1^+} \frac{x^2 - 1}{x - 1} = \lim_{x \to 1^+} \frac{(x - 1)(x + 1)}{x - 1} = \lim_{x \to 1^+} (x + 1) = 2 \]
    \item Για $x \to 1^-$, ισχύει $x < 1 \Rightarrow x^2 - 1 < 0$, οπότε $|x^2 - 1| = -(x^2 - 1)$:
    \[ \lim_{x \to 1^-} \frac{-(x^2 - 1)}{x - 1} = \lim_{x \to 1^-} \frac{-(x - 1)(x + 1)}{x - 1} = \lim_{x \to 1^-} -(x + 1) = -2 \]
\end{itemize}
Επειδή τα πλευρικά όρια είναι άνισα ($-2 \neq 2$), το όριο $\lim\limits_{x \to 1} \dfrac{\sqrt{f(x)}}{x-1}$ \textbf{δεν υπάρχει}.
\end{erwthma}
\end{thema}

%=========== ΤΕΛΟΣ ΛΥΣΗΣ - ΘΕΜΑ Γ ===========