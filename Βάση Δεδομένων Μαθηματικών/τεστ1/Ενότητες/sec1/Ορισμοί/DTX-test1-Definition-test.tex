%# Database File : DTX-test1-Definition-test
\begin{asy}
settings.outformat="pdf";
import graph;

size(8cm,0);

draw((0,0) -- (5,0), arrow=Arrow); 
draw((0,-0.5) -- (0,5), arrow = Arrow);

real f(real x) { return x/log(x); };
path pft = graph(f, 1.5, 5);

draw(pft);
\end{asy}
%# End of file DTX-test1-Definition-test