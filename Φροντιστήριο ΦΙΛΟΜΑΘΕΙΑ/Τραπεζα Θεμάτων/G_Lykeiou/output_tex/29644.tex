\Askhsh{29644}{4}
\begin{ylh}{1}
    \item 1.8 Συνέχεια συνάρτησης
    \item 2.2 Παραγωγίσιμες συναρτήσεις - Παράγωγος συνάρτηση
    \item 2.7 Τοπικά ακρότατα συνάρτησης.
\end{ylh}
\begin{wrapfigure}[25]{r}{8cm} % Increased lines to 25 to accommodate the figure
    \centering
    \begin{tikzpicture}
    \begin{axis}[
        axis lines=middle,
        xmin=-3.5, xmax=2.5,
        ymin=-1, ymax=3.5,
        xtick={-3, 2},
        ytick={3},
        extra x ticks={0}, % To ensure a tick mark at 0 for the origin
        extra y ticks={0}, % To ensure a tick mark at 0 for the origin
        xticklabels={-3, 2},
        yticklabels={3},
        xlabel={$x$},
        ylabel={$y$},
        axis on top, % Ensure axis lines are drawn over the plot
        every axis x label/.style={at={(current axis.right of origin)}, anchor=north west},
        every axis y label/.style={at={(current axis.above origin)}, anchor=south east},
        clip=false, % Allow drawing outside the axis limits for dashed lines
        height=7cm, width=7cm, % Explicitly set dimensions for the plot area
    ]
        % Plot the function f(x)
        \addplot[very thick, maincolor, smooth] coordinates {
            (-3,0)
            (-2.2, 0.8) % Adjusted points to better match the visual curve
            (-1.5, 1.5)
            (-1, 1.5)
            (-0.5, 2.2)
            (0,3)
            (0.5, 2.2)
            (1.5, 0.8)
            (2,0)
        };

        % Label points A, B, O
        \node[below left] at (axis cs:-3,0) {A};
        \node[below right] at (axis cs:2,0) {B};
        \node[below left] at (axis cs:0,0) {O}; % Label origin

        % Dashed lines at x=-3 and x=2, extending slightly below x-axis
        \draw[dashed] (axis cs:-3,0) -- (axis cs:-3,-0.8);
        \draw[dashed] (axis cs:2,0) -- (axis cs:2,-0.8);

        % Mark points on the x-axis and the maximum
        \fill (axis cs:-3,0) circle (1.5pt);
        \fill (axis cs:2,0) circle (1.5pt);
        \fill (axis cs:0,3) circle (1.5pt); % Mark the maximum point
    \end{axis}
    \end{tikzpicture}
\end{wrapfigure}

Στο παρακάτω σχήμα δίνεται η γραφική παράσταση μιας συνεχούς συνάρτησης $f$ στο διάστημα $[-3,2]$ η οποία παρουσιάζει μέγιστο στο $0$ το $3$ και τέμνει τον άξονα $x'x$ στα σημεία $A$ και $B$. Έστω $g$ η συνάρτηση με $g(x) = f(x) + x, x \in [-3,2]$.

\begin{alist}
    \item Να αποδείξετε ότι:
    \begin{rlist}
        \item Η συνάρτηση $g$ είναι συνεχής στο $[-3,2]$. \monades{05}
        \item Η εξίσωση $g(x) = 0$ έχει μία τουλάχιστον ρίζα. \monades{10}
    \end{rlist}
    \item Αν η συνάρτηση $f$ είναι παραγωγίσιμη στο $(-1,2)$, να αποδείξετε ότι η εφαπτομένη ευθεία της γραφικής παράστασης της συνάρτησης $g$, στο σημείο που η $f$ παρουσιάζει μέγιστο, έχει εξίσωση $y = x + 3$. \monades{10}
\end{alist}