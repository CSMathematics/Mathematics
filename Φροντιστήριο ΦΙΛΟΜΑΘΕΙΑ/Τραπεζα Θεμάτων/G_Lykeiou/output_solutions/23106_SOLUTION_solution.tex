\Askhsh{23106_SOLUTION}{1}
\begin{ylh}{1}
    \item [Ύλη από εικόνα]
\end{ylh}
ΛΥΣΗ

\begin{alist}
    \item[α)]
    \begin{rlist}
        \item Έχουμε:
        \[ g(f(x)) = |\συν x| \Leftrightarrow \sqrt{1-f^2(x)} = |\συν x| \Leftrightarrow 1-f^2(x) = \συν^2 x \Leftrightarrow \]
        \[ f^2(x) = 1-\συν^2 x \Leftrightarrow f^2(x) = \ημ^2 x \Leftrightarrow |f(x)| = |\ημ x| \quad (1) \]
        \item Για τις ρίζες της εξίσωσης $f(x)=0$ ισχύει ότι:
        \[ f(x) = 0 \Leftrightarrow |f(x)| = 0 \overset{(1)}{\Leftrightarrow} |\ημ x| = 0 \Leftrightarrow \ημ x = 0 \Leftrightarrow \]
        \[ \left\{ \begin{array}{l} x = 2\kappa\pi + 0 \\ x = 2\kappa\pi + \pi - 0 \end{array} \right., \kappa \in \mathbb{Z} \Leftrightarrow \left\{ \begin{array}{l} x = 2\kappa\pi \\ x = (2\kappa+1)\pi \end{array} \right. \Leftrightarrow x = \lambda\pi, \lambda \in \mathbb{Z}. \]
        Επειδή όμως η $f$ είναι ορισμένη στο $[0, \pi]$, τότε έχουμε: $0 \leq \lambda\pi \leq \pi \Leftrightarrow 0 \leq \lambda \leq 1.$
        Επομένως είναι $x=0$ ή $x=\pi$.
    \end{rlist}
    \item[β)] Ως γνωστόν, αν μία συνάρτηση είναι συνεχής σε ένα διάστημα $\Delta$ και δε μηδενίζεται σε αυτό, τότε διατηρεί σταθερό πρόσημο στο διάστημα $\Delta$.
    Επομένως η $f$ διατηρεί σταθερό πρόσημο στο διάστημα $(0, \pi)$, δηλαδή $f(x) > 0$ για κάθε $x \in (0, \pi)$ ή $f(x) < 0$ για κάθε $x \in (0, \pi)$.
    Επειδή όμως $\ημ x > 0$ για κάθε $x \in (0, \pi)$ και $f(0) = f(\pi) = 0$, από την (1) έχουμε ισοδύναμα ότι
    \[ f(x) = \ημ x, x \in [0, \pi] \text{ ή } f(x) = -\ημ x, x \in [0, \pi]. \]
    Αλλά $f\left(\frac{\pi}{2}\right) = 1$, επομένως η ζητούμενη συνάρτηση, είναι η
    \[ f(x) = \ημ x, x \in [0, \pi]. \]
    \item[γ)] Είναι $h(x) = \frac{1}{\ημ x - x}, x \in (0,\pi].$
    Παρατηρούμε ότι
    \[ \lim_{x \to 0} (\ημ x - x) = 0 \]
    Είναι γνωστό ότι $|\ημ x| \leq |x|$, για κάθε $x \in \mathbb{R}$, με την ισότητα να ισχύει μόνο όταν $x=0$.
    Επειδή $x \in (0,\pi]$, είναι $\ημ x \geq 0$ και $x > 0$, επομένως έχουμε:
    \[ \ημ x < x \Leftrightarrow \ημ x - x < 0. \]
    Άρα
    \[ \lim_{x \to 0} \frac{1}{\ημ x - x} = -\infty \]
\end{alist}