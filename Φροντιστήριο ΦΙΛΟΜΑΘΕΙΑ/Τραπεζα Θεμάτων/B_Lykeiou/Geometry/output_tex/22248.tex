\Askhsh{22248}{2}
\begin{ylh}{1}
    \item 7.4. Ανάλογα ευθύγραμμα τμήματα - Αναλογίες
    \item 7.7. Θεώρημα του Θαλή
    \item 9.4. Γενίκευση του Πυθαγόρειου θεωρήματος.
\end{ylh}
\begin{wrapfigure}[19]{r}{5cm} % Adjust line count as needed to fit the text
    \centering
    \begin{tikzpicture}[scale=0.5] % Scale adjusted to fit
        % Define points A, B, C based on calculated coordinates for angle A = 90 degrees
        % A is origin (bottom-left)
        \tkzDefPoint(0,0){A}
        % B is on y-axis (top-left)
        \tkzDefPoint(0,9){B}
        % C is on x-axis (bottom-right)
        \tkzDefPoint(12,0){C}
        
        % Define points E and D based on problem statement
        % E on CA, EA=4. Since A=(0,0) and C=(12,0), E is (4,0)
        \tkzDefPoint(4,0){E}
        % D on CB and on line epsilon (x=4). Calculated as D=(4,6)
        \tkzDefPoint(4,6){D}
        
        % Define an auxiliary point Cprime to draw line delta parallel to AB through C
        % AB is the y-axis (x=0). So delta is x=12. Cprime can be (12, 9) or similar.
        \tkzDefPoint(12,9){Cprime} % A point directly above C to define the vertical line delta.
        
        % Draw the triangle ABC
        \tkzDrawSegments(A,B B,C C,A)
        
        % Draw line epsilon (through D and E, parallel to AB)
        % Using 'add' to extend the dashed line beyond the points
        \tkzDrawLine[add=1 and 1,dashed](D,E)
        
        % Draw line delta (through C and Cprime, parallel to AB)
        % Using 'add' to extend the dashed line beyond the points
        \tkzDrawLine[add=1 and 1,dashed](C,Cprime)
        
        % Draw points
        \tkzDrawPoints(A,B,C,D,E)
        
        % Label points
        \tkzLabelPoint[below left](A){A}
        \tkzLabelPoint[above left](B){B}
        \tkzLabelPoint[below right](C){$\Gamma$}
        \tkzLabelPoint[below](E){E}
        \tkzLabelPoint[above right](D){$\Delta$}
        
        % Label lines
        \tkzLabelLine[pos=0.5, right=2mm](D,E){$\varepsilon$}
        \tkzLabelLine[pos=0.5, right=2mm](C,Cprime){$\delta$}
    \end{tikzpicture}
\end{wrapfigure}
Θεωρούμε τρίγωνο ΑΒΓ με $AB = 9$, $\Gamma A = 12$ και $\Gamma B = 15$ και ευθείες $\varepsilon$, $\delta$ παράλληλες στην ΑΒ, όπως αυτές του σχήματος.
\begin{alist}
    \item Να αποδείξετε ότι το τρίγωνο ΑΒΓ είναι ορθογώνιο και να βρείτε ποια πλευρά είναι η υποτείνουσά του. \monades{8}
    \item Αν η ευθεία ($\varepsilon$) τέμνει τις πλευρές $\Gamma A$, $\Gamma B$ σε σημεία Ε και $\Delta$ αντίστοιχα έτσι ώστε $EA = 4$ και η ευθεία ($\delta$) διέρχεται από το σημείο $\Gamma$, τότε να υπολογίσετε
    \begin{rlist}
        \item το τμήμα $\Delta B$, \monades{8}
        \item τις πλευρές του τριγώνου $\Delta E \Gamma$. \monades{9}
    \end{rlist}
\end{alist}