\Askhsh{16805}{2}
\begin{ylh}{1}
    \item 7.4. Ανάλογα ευθύγραμμα τμήματα - Αναλογίες
    \item 9.2. Το Πυθαγόρειο θεώρημα.
\end{ylh}
\begin{wrapfigure}[12]{r}{9cm} % Number of lines and width adjusted for the diagram
    \centering
    \begin{tikzpicture}[scale=0.35] % Scale adjusted for better fit
        % Define points based on the problem's geometric setup
        % A is bottom-left, B is bottom-right, Gamma is top-right, Delta is top-left
        % E is on AB. x = AE, y = EB, z = BG (or AD)
        % For the diagram, we use general coordinates as the values are part of the problem.
        % Let A=(0,0). E=(x,0). B=(x+y,0). G=(x+y,z). D=(0,z).
        % We don't use the specific values (8, 16, 12) for point definition in the figure code itself,
        % as those are derived in the solution part of the exercise, not the setup.
        % However, for drawing, concrete values are needed. We use placeholder values that are
        % proportional to 2,4,3, e.g., x=2, y=4, z=3, for the visual representation.
        % To make the figure visually proportional to the *expected* actual values (8,16,12),
        % let's use a scale factor for the points internally.
        % Using the *expected* values for coordinates directly makes the diagram accurate.
        % x=8, y=16, z=12 (from analysis for part a)
        \tkzDefPoint(0,0){A}
        \tkzDefPoint(8,0){E}
        \tkzDefPoint(24,0){B} % x+y = 8+16 = 24
        \tkzDefPoint(24,12){G} % z = 12
        \tkzDefPoint(0,12){D}

        % Draw the rectangle ABGD
        \tkzDrawPolygon(A,B,G,D)

        % Draw the segments DE and GE
        \tkzDrawSegments(D,E G,E)

        % Label the points
        \tkzLabelPoint[below left](A){A}
        \tkzLabelPoint[below](E){E}
        \tkzLabelPoint[below right](B){B}
        \tkzLabelPoint[above right](G){$\Gamma$}
        \tkzLabelPoint[above left](D){$\Delta$}

        % Label the lengths x, y, z
        \tkzLabelSegment[below](A,E){$x$}
        \tkzLabelSegment[below](E,B){$y$}
        \tkzLabelSegment[right](B,G){$z$}
        
        % Draw the points
        \tkzDrawPoints(A,B,G,D,E)
    \end{tikzpicture}
\end{wrapfigure}
\kefalaio{ΘΕΜΑ 2}
Η περίμετρος του ορθογωνίου ΑΒΓΔ του σχήματος είναι 72 και το Ε είναι σημείο στην πλευρά ΑΒ. Τα μήκη των τμημάτων $x$, $y$, $z$ είναι ανάλογα προς τους αριθμούς $2,4,3$ αντίστοιχα.
\begin{alist}
    \item Να αποδείξετε ότι $x = 8$, $y = 16$ και $z = 12$. \monades{13}
    \item Να υπολογίσετε την περίμετρο του τριγώνου ΓΕΔ. \monades{12}
\end{alist}