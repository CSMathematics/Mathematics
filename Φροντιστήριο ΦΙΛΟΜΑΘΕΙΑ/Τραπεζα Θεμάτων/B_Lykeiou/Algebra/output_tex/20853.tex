% ===================================================================
%  Αρχείο: 20853.tex (Fragment)
%  Αυτόματη μετατροπή μέσω doc2latex.py
% ===================================================================

ΘΕΜΑ 2

Δίνεται η συνάρτηση $f(x) = \ln(x - 1)$ και η γραφική παράσταση της συνάρτησης

$g(x) = \ln x,\ \ x > 0$.


% TODO: Μετατροπή σχήματος σε TikZ/PGFPlots
\begin{center}

\begin{tikzpicture}[scale=1]
    % Grid
    \draw[very thin, lightgray] (-1,-3) grid (6,3);
    
    % Axes
    \draw[->, thick] (-1.5,0) -- (6.5,0) node[right] {$x$};
    \draw[->, thick] (0,-3.5) -- (0,3.5) node[above] {$y$};
    
    % Logarithmic curve y = log2(x)
    \draw[ultra thick, blue, domain=0.125:6, samples=100] plot (\x, {ln(\x)/ln(2)}) node[above] {$C_g$};
    
    % Key Points
    \filldraw (1,0) circle (2pt) node[below right] {$1$};
    \filldraw (2,1) circle (2pt);
    \draw[dashed] (2,1) -- (2,0) node[below] {$2$};
    \draw[dashed] (2,1) -- (0,1) node[left] {$1$};
    
    % Origin
    \node[below left] at (0,0) {$O$};

\end{tikzpicture}

% % \caption{Σχήμα 1}
\end{center}


\begin{alist}
\item Να βρείτε το πεδίο ορισμού της συνάρτησης $f$.

\monades{05}

\item Με τη βοήθεια της γραφικής παράστασης της συνάρτησης $g$, να σχεδιάσετε την γραφική παράσταση της συνάρτησης $f$.

\monades{08}

\item Να βρείτε το διάστημα, στο οποίο η γραφική παράσταση της συνάρτησης $f$ βρίσκεται κάτω από τον άξονα $x΄x$.

\monades{12}
\end{alist}
