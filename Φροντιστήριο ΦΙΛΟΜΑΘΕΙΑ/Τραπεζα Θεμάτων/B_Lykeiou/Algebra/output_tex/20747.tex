% ===================================================================
%  Αρχείο: 20747.tex (Fragment)
%  Αυτόματη μετατροπή μέσω doc2latex.py
% ===================================================================

ΘΕΜΑ 4

Δίνεται η συνάρτηση $f(x) = 3 + 3\varepsilon\varphi\omega\cdot \eta\mu x, x \in \mathbb{R}$. Αν για τη γωνία ω ισχύει η σχέση $-2\sigma\upsilon\nu^2\omega + \eta\mu\omega = -1, \omega \in \left[0, \frac{\pi}{2}\right]$, τότε:

\begin{alist}
\item

\end{alist}
\begin{alist}
\item
  Να αποδείξετε ότι $\varepsilon\varphi\omega = \frac{\sqrt{3}}{3}$.

\monades{10}


\end{alist}
\begin{alist}
\item
  Για $\varepsilon\varphi\omega = \frac{\sqrt{3}}{3}$, να βρείτε τη μέγιστη και την ελάχιστη τιμή της συνάρτησης f.

\monades{04}

\item Δίνεται η συνάρτηση $g(x) = x^2 - 10x + 30, x \in \mathbb{R}$ και η γραφική της παράσταση στο παρακάτω σχήμα.


% TODO: Μετατροπή σχήματος σε TikZ/PGFPlots
\begin{center}

\begin{tikzpicture}[scale=0.8]
    % Grid
    \draw[very thin, lightgray] (0,0) grid (10,12);
    
    % Axes
    \draw[->, thick] (-1,0) -- (11,0) node[right] {$x$};
    \draw[->, thick] (0,-1) -- (0,13) node[above] {$y$};
    
    % Parabola y = (x-5)^2 + 5 = x^2 - 10x + 30
    \draw[ultra thick, secondarycolor, domain=2.5:7.5, samples=100] plot (\x, {(\x-5)*(\x-5) + 5});
    
    % Vertex (5,5)
    \filldraw[blue] (5,5) circle (3pt) node[above right, black] {$(5,5)$};
    \draw[dashed] (5,5) -- (5,0) node[below] {$5$};
    \draw[dashed] (5,5) -- (0,5) node[left] {$5$};

    % Domain limits (0 to 10 context)
    \node[below] at (0,0) {$0$};
    \node[below] at (10,0) {$10$};

\end{tikzpicture}

% % \caption{Σχήμα 1}
\end{center}


\end{alist}
\begin{alist}
\item
  Να βρείτε, με τη βοήθεια της γραφικής παράστασης ή με οποιονδήποτε άλλο τρόπο, την ελάχιστη τιμή της συνάρτησης g.

\monades{04}


\end{alist}
\begin{alist}
\item
  Να εξετάσετε αν οι γραφικές παραστάσεις των συναρτήσεων f, g έχουν κοινά σημεία. Να αιτιολογήσετε την απάντησή σας.

\monades{07}
\end{alist}
