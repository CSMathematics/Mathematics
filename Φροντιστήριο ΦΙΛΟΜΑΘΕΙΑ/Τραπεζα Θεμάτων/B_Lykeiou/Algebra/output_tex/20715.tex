% ===================================================================
%  Αρχείο: 20715.tex (Fragment)
%  Αυτόματη μετατροπή μέσω doc2latex.py
% ===================================================================

ΘΕΜΑ 4

Με συρματόπλεγμα μήκους 20 m θέλουμε να περιφράξουμε οικόπεδο σχήματος ορθογωνίου με διαστάσεις $x$ και $y$, όπως φαίνεται στο παρακάτω σχήμα.

\begin{alist}
\item Να εκφράσετε την πλευρά $y$ ως συνάρτηση της πλευράς $x$ και να βρείτε τις δυνατές τιμές της πλευράς $x$.

\monades{7}

\item Να αποδείξετε ότι το εμβαδόν $E(x)$ του ορθογωνίου ως συνάρτηση του x δίνεται από τη συνάρτηση $E(x) = -(x - 5)^2 + 25$ και να βρείτε το πεδίο ορισμού της στο πλαίσιο του προβλήματος.

\monades{7}

\item Παρακάτω δίνεται η γραφική παράστασης της συνάρτησης $g(x) = -x^2$. Μετατοπίζοντάς τη κατάλληλα, να σχεδιάσετε τη γραφική παράσταση της συνάρτησης $E(x)$ και με βάση αυτή, να βρείτε το $x$ έτσι ώστε το εμβαδόν $E(x)$ του ορθογωνίου να γίνεται μέγιστο.

\monades{7}

\item Για την τιμή του $x$ που βρήκατε στο ερώτημα γ), να βρείτε την πλευρά $y$ και να προσδιορίσετε το είδος του ορθογωνίου.

\monades{4}

% TODO: Μετατροπή σχήματος σε TikZ/PGFPlots
\begin{center}
\begin{tikzpicture}[scale=0.15]
    % Grid
    \draw[very thin, lightgray] (-25,-30) grid[step=5] (30,5);
    
    % Axes
    \draw[->, thick] (-25,0) -- (30,0) node[right] {$x$};
    \draw[->, thick] (0,-30) -- (0,5) node[above] {$y$};
    
    % Origin
    \node[below left] at (0,0) {$O$};

    % Function y = -x^2 / 20 (scaled to fit labels and logic)
    % The original image shows a parabola opening down. 
    % Based on the labels -30 to 45, it marks a physical/model scenario.
    \draw[ultra thick, maincolor, domain=-20:25, samples=100] plot (\x, {-0.04*\x*\x});

    % Ticks every 5
    \foreach \x in {-20, -10, 10, 20, 30}
        \draw (\x, 1) -- (\x, -1) node[below, font=\tiny] {$\x$};
    \foreach \y in {-30, -20, -10,}
        \draw (1, \y) -- (-1, \y) node[left, font=\tiny] {$\y$};

\end{tikzpicture}

% % \caption{Σχήμα 1}
\end{center}
\end{alist}
